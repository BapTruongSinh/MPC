# Review Memory

File này lưu tóm tắt ngắn gọn sau mỗi plan, việc, cửa sổ làm việc, hoặc prompt để lần sau đọc lại nhanh.

## Quy ước cập nhật

- Ghi ngắn, đủ ý, không biến file này thành nhật ký dài.
- Mỗi entry nên có: ngày, người/agent, việc đã làm, file thay đổi, verification, rủi ro còn lại, bước tiếp theo.
- Trước khi bắt đầu việc mới, đọc file này cùng các rule trong `.claude/.claude/rules/`.

## Log

| Date | Agent / Human | Summary | Files | Verification | Residual risk | Next |
|------|---------------|---------|-------|--------------|---------------|------|
| 2026-05-12 | Codex | Addressed Green-House #004 review findings. Client validation now rejects non-finite numbers, negative `crop_kc`, negative cost weights, non-positive pump max, and non-positive soft daily cap. Backend serializers now also reject invalid runtime config values for the legacy and greenhouse control-profile paths, so server remains authoritative. Frontend smoke test now executes `autoSettingsConfig` behavior through Vite SSR and server-renders `AutoSettingsForm` instead of relying only on string checks. | `Green-House/frontend/src/app/components/{AutoSettings.tsx,autoSettingsConfig.ts}`, `Green-House/frontend/scripts/smoke-test.mjs`, `Green-House/backend/api/{serializers.py,tests/test_server_cutover.py}`, `Green-House/.tasks/004-fao-auto-settings-frontend.md`, `.claude/.claude/review/REVIEW.md` | `cd Green-House\frontend; npm test` -> passed; `npm run build` -> passed; targeted auto-settings Django tests -> 3 passed; `python manage.py check` -> OK; `python manage.py makemigrations --check --dry-run` -> no changes; `python manage.py test api -v 1 --noinput` -> 31 passed; `python -m compileall -q api config` -> OK; `python -m pytest MPC\tests -q` -> 117 passed. | The frontend still does not use a full browser/component test runner such as Vitest + Testing Library; current executable smoke coverage tests pure behavior and server-rendered loaded form output. `CODEBASE_ONBOARDING.md` remains intentionally unchanged until owner review approval. | Re-review #004, then continue with task #005. |
| 2026-05-12 | Codex | Completed Green-House task #004: frontend Auto Settings now exposes FAO-56 physical config controls, typed soil presets, editable theta values after preset selection, client-side validation, API validation error display, and save payload limited to supported auto-settings fields. Added smoke coverage for FAO fields/presets/payload/error handling and documented the frontend settings contract. | `Green-House/frontend/src/app/api/endpoints.ts`, `Green-House/frontend/src/app/components/{AutoSettings.tsx,autoSettingsConfig.ts}`, `Green-House/frontend/scripts/smoke-test.mjs`, `Green-House/docs/technical/AMPC_MODELING_HANDOFF.md`, `Green-House/TODO.md`, `Green-House/.tasks/004-fao-auto-settings-frontend.md`, `.claude/.claude/review/REVIEW.md` | `cd Green-House\frontend; npm test` -> passed; `npm run build` -> passed; `git diff --check -- Green-House` -> no whitespace errors, only LF/CRLF warnings. | Frontend test coverage remains static smoke-style because the project has no component test runner yet. Forecast FAO audit display remains task #005. `Green-House/docs/technical/CODEBASE_ONBOARDING.md` intentionally not updated until owner review approval. | Review #004 UI, then implement #005 FAO forecast/dashboard audit display. |
| 2026-05-12 | Codex | Addressed Green-House #003 review finding: AMPC runtime now fails closed if an invalid FAO profile is already in DB and bypasses serializer validation. `run_auto_recommendation()` catches `profile_to_config()` failures before ET0/solver, persists an `AMPCRecommendation` with `safety_status="config_error"`, pump `0`, `state_snapshot.fail_closed=true`, skips ET0, and queues no actuator command even in AUTO. Added regression coverage using direct DB update to `root_depth_m=0`. | `Green-House/backend/api/{ampc.py,tests/test_server_cutover.py}`, `Green-House/.tasks/003-wire-ampc-fao-runtime.md`, `.claude/.claude/review/REVIEW.md` | Targeted invalid-profile test -> passed; `cd Green-House\backend; python manage.py test api -v 1 --noinput` -> 30 passed; `python manage.py check` -> OK; `python manage.py makemigrations --check --dry-run` -> no changes; `python -m compileall -q api config` -> OK; `python -m pytest MPC\tests -q` -> 117 passed; `python -m compileall -q MPC\mpc` -> OK; `git diff --check -- Green-House MPC .claude\.claude\review\REVIEW.md` -> no whitespace errors, only LF/CRLF warnings. | `CODEBASE_ONBOARDING.md` still intentionally not updated until owner review approval. Invalid profile audit uses default MPC config only to shape the fail-closed recommendation payload; the invalid values remain visible in `config_snapshot`. | Re-review #003, then continue with Green-House #004/#005/#006. |
| 2026-05-12 | Codex | Completed Green-House task #003: wired AMPC runtime to FAO-56 water-balance mode. `profile_to_config()` now builds MPC `Fao56Config` from `GreenhouseControlProfile`; `run_auto_recommendation()` fetches ET0 before solving, overrides `fao56.et0_hour_mm`, fails closed with pump `0` when ET0 is unavailable, and persists ET0 plus FAO audit details in `AMPCRecommendation.state_snapshot` while preserving existing recommendation fields. Legacy greenhouse recommendation responses now include `state_snapshot` for backward-compatible audit access. | `Green-House/backend/api/{ampc.py,serializers.py,tests/test_server_cutover.py}`, `Green-House/docs/technical/AMPC_MODELING_HANDOFF.md`, `Green-House/TODO.md`, `Green-House/.tasks/003-wire-ampc-fao-runtime.md`, `.claude/.claude/review/REVIEW.md` | Targeted FAO runtime tests -> 4 passed; `cd Green-House\backend; python manage.py test api -v 1 --noinput` -> 29 passed; `python manage.py check` -> OK; `python manage.py makemigrations --check --dry-run` -> no changes; `python -m compileall -q api config` -> OK; `python -m pytest MPC\tests -q` -> 117 passed; `python -m compileall -q MPC\mpc` -> OK; `git diff --check -- Green-House MPC .claude\.claude\review\REVIEW.md` -> no whitespace errors, only LF/CRLF warnings. | `Green-House/docs/technical/CODEBASE_ONBOARDING.md` intentionally not updated until owner review approval. Frontend controls/audit presentation remain tasks #004/#005. `cost_breakdown.band` in MPC remains backward-compatible naming for FAO stress cost. Existing unrelated staged/dirty work in Green-House and MPC was preserved. | Review #003, then implement frontend #004/#005 and final QA/docs #006. |
| 2026-05-12 | Codex | Addressed MPC #012 review finding: simulation report `objective_cost` no longer uses legacy sensor target-band scoring. Added a shared FAO daily-reset scorer for executed pump sequences, wired simulation metrics to score from initial sensor percent through FAO `Dr/RAW` stress and overwater terms, and strengthened regression tests to compare the public report against the exact FAO formula. | `MPC/mpc/solver/cost.py`, `MPC/mpc/simulation/runner.py`, `MPC/tests/test_simulation_regression.py`, `MPC/docs/technical/{API.md,VALIDATION.md}`, `MPC/docs/user/USER_GUIDE.md`, `MPC/.tasks/012-fao56-dr-objective-recommendation.md`, `.claude/.claude/review/REVIEW.md` | `python -m pytest MPC\tests\test_simulation_regression.py MPC\tests\test_fao56.py -q` -> 39 passed; `python -m compileall -q MPC\mpc` -> OK; `python -m pytest MPC\tests -q` -> 117 passed; `git diff --check -- MPC` -> no whitespace errors, only LF/CRLF warnings. Formula check: report `objective_cost` now equals `score_fao56_pump_sequence_with_daily_reset(...).cost.total`; `band_violation_*` remains only a legacy dashboard diagnostic. | `cost_breakdown.band` key is retained for backward compatibility but now contains FAO stress cost, matching existing cost dataclass naming. `CODEBASE_ONBOARDING.md` remains intentionally unchanged until owner review approval. | Re-review/accept #012, then implement Green-House #003 AMPC FAO runtime wiring. |
| 2026-05-12 | Codex | Completed MPC task #012: switched grid solver scoring from legacy percent target band to FAO `Dr/TAW/RAW` control, added `et0_hour_mm`, retained `predicted_soil_moisture` as compatibility output, and added backward-compatible recommendation `fao56` audit details. Updated simulation reporting, tests, API/config/validation/user docs, task state, and TODO. | `MPC/mpc/{fao56.py,solver/cost.py,solver/grid.py,types.py,schema.py,simulation/report.py}`, `MPC/tests/{test_fao56.py,test_grid_solver.py,test_actuator_closed_loop.py,test_cli_simulation.py,test_simulation_regression.py}`, `MPC/docs/technical/{API.md,CONFIG.md,VALIDATION.md}`, `MPC/docs/user/USER_GUIDE.md`, `MPC/TODO.md`, `MPC/.tasks/012-fao56-dr-objective-recommendation.md`, `.claude/.claude/review/REVIEW.md` | `python -m pytest MPC\tests\test_grid_solver.py -q` -> 16 passed; `python -m pytest MPC\tests\test_fao56.py MPC\tests\test_cli_simulation.py -q` -> 51 passed; `python -m pytest MPC\tests -q` -> 117 passed; `python -m compileall -q MPC\mpc` -> OK; CLI smoke from `MPC/`: `recommend`, `simulate`, `adaptive-simulate`, `auto`, and `config-schema` all OK; `git diff --check -- MPC` -> no whitespace errors, only LF/CRLF warnings. | `MPC/docs/technical/CODEBASE_ONBOARDING.md` intentionally not updated until owner review approval. Green-House #003 remains todo and should now be unblocked by MPC #011/#012. Simulation report keeps legacy band metrics for compatibility while objective cost is FAO-based. Existing unrelated dirty Green-House changes and staged `views.py` changes were preserved. | Implement Green-House #003 AMPC FAO runtime wiring from persisted profile + ET0 + MPC `fao56` audit contract. |
| 2026-05-12 | Codex | Reviewed MPC task #011 after implementation. No blocking findings found: `mpc.fao56` is a pure package module, soil presets and defaults match the accepted FAO plan, sensor percent maps through `theta_wp/theta_sat`, TAW/RAW/Dr/Ks/ET0/ETc/irrigation/next-Dr formulas are dimensionally consistent, config/schema export includes `fao56`, and invalid/non-finite FAO config is rejected. | `.claude/.claude/review/REVIEW.md`; reviewed `MPC/mpc/{fao56.py,config.py,schema.py,__init__.py}`, `MPC/tests/test_fao56.py`, `MPC/docs/technical/{CONFIG.md,API.md}`, `MPC/.tasks/011-fao56-water-balance-model.md`, `MPC/TODO.md`, and accepted FAO plan. | `python -m pytest MPC\tests\test_fao56.py -q` -> 33 passed; `python -m pytest MPC\tests -q` -> 115 passed; `python -m compileall -q MPC\mpc` -> OK; schema probe confirmed `fao56.*` defaults/options; invalid config probe rejected NaN, zero pump flow, bad soil type, and non-string soil type; `git diff --check` on #011 files -> OK except expected CRLF warnings. | #011 only provides primitives; solver objective still uses legacy target-band path until #012. `MPC/docs/technical/CODEBASE_ONBOARDING.md` intentionally remains stale until owner review acceptance per rule. | Accept #011, then implement/review MPC #012 before returning to Green-House #003 runtime wiring. |
| 2026-05-12 | Codex | Completed MPC task #011 FAO-56 water-balance model. Added pure `mpc.fao56` contracts for soil/crop/hydraulic config, exact accepted soil presets, sensor-percent-to-theta mapping, TAW/RAW/Dr/Ks/ET/I/next-Dr formulas, non-finite/physical validation, config loading/schema export, docs, and backlog sync. Kept solver target-band path untouched for #012. | `MPC/mpc/{fao56.py,config.py,schema.py,__init__.py}`, `MPC/tests/test_fao56.py`, `MPC/docs/technical/{CONFIG.md,API.md}`, `MPC/.tasks/011-fao56-water-balance-model.md`, `MPC/TODO.md`, `.claude/.claude/review/REVIEW.md` | `python -m pytest MPC\tests\test_fao56.py -q` -> 33 passed; `python -m pytest MPC\tests -q` -> 115 passed; `python -m compileall -q MPC\mpc` -> OK; `git diff --check -- MPC` -> no whitespace errors, only LF/CRLF warnings. | #011 only creates reusable primitives; MPC recommendation still uses old target-band objective until #012. Green-House runtime wiring remains blocked until #012 completes. Existing unrelated dirty Green-House changes and staged `views.py` changes were left untouched. | Implement MPC #012 next, then return to Green-House #003 runtime wiring. |
| 2026-05-12 | Codex | Reviewed rollback state before starting MPC #011 after owner rolled back premature Green-House #003 work. Confirmed Green-House #003 remains `todo` and blocked by `MPC-011`/`MPC-012`; no source references remain for FAO runtime wiring such as `get_hourly_et0` in `ampc.py`, FAO audit fields (`initial_dr`, `taw`, `raw`, `ks`, `etc_adj`, `predicted_dr`), or MPC `fao56`/`water_balance` source modules. Current source changes are #001 config, #002 ET0 service, task/backlog docs, plus older staged `views.py` test-series hardening. | `.claude/.claude/review/REVIEW.md`; inspected `Green-House/backend/api/{ampc.py,ampc_scheduler.py,views.py,tests/test_server_cutover.py,et0.py}`, `Green-House/.tasks/003-wire-ampc-fao-runtime.md`, `MPC/{TODO.md,.tasks/011-fao56-water-balance-model.md,mpc/,tests/}`. | `rg` for task #003/#011 runtime symbols found only expected #001/#002/task-doc matches; `Test-Path MPC\mpc\fao56.py` and `water_balance.py` -> false; `import mpc.fao56` -> `ModuleNotFoundError`; `python -m pytest MPC\tests -q` -> 82 passed; `cd Green-House\backend; python manage.py check` -> OK. | Ignored local artifact `MPC/mpc/__pycache__/fao56.cpython-310.pyc` remains from the rolled-back attempt; it is not importable and not tracked, but should be deleted before #011 for a clean tree. `views.py` still has pre-existing staged test-series changes unrelated to FAO #003. | Clean ignored pycache if desired, then start MPC #011 as the correct prerequisite before Green-House #003. |
| 2026-05-12 | Codex | Reviewed Green-House task #002 Open-Meteo ET0 service. No blocking findings found: implementation matches the accepted plan and official Open-Meteo `/v1/forecast` hourly `et0_fao_evapotranspiration` contract, uses UTC hourly lookup, validates finite non-negative ET0, computes `ET0_step`, bounds external HTTP reads/timeouts, caches exact/recent readings, and returns typed fail-closed `ET0Failure` when no valid ET0 is available. | `.claude/.claude/review/REVIEW.md`; reviewed `Green-House/backend/api/et0.py`, `Green-House/backend/api/tests/test_et0.py`, `Green-House/.tasks/002-open-meteo-et0-service.md`, `Green-House/TODO.md`. | Official Open-Meteo docs checked for `/v1/forecast`, `hourly`, `timezone`, `start_date/end_date`, API key, and ET0 hourly semantics; `cd Green-House\backend; python manage.py test api.tests.test_et0 -v 2` -> 5 passed; `python manage.py test api -v 1 --noinput` -> 26 passed; `python manage.py check` -> OK; `python manage.py makemigrations --check --dry-run` -> no changes; `python -m py_compile api\et0.py api\tests\test_et0.py` -> OK; `python -m pytest Kalman\tests -q` -> 15 passed; `python -m pytest MPC\tests -q` -> 82 passed; `git diff --check` on #002 files -> OK. | Task #002 is not wired into AMPC execution yet by design; task #003 must persist audit/fail-closed recommendation behavior. Cache is Django app cache, so ET0 fallback does not survive process/cache restart unless later moved to DB. | Accept #002, then implement #003 AMPC runtime wiring from profile + ET0 + FAO water-balance MPC. |
| 2026-05-12 | Codex | Completed Green-House task #002: added an Open-Meteo ET0 service that fetches hourly `et0_fao_evapotranspiration` by greenhouse coordinates, buckets lookup in UTC, caches exact hourly values, falls back to recent valid cache on external failure, validates finite non-negative ET0, converts to `ET0_step`, and returns typed fail-closed results with pump off when unavailable. | `Green-House/backend/api/et0.py`, `Green-House/backend/api/tests/test_et0.py`, `Green-House/.tasks/002-open-meteo-et0-service.md`, `Green-House/TODO.md`, `.claude/.claude/review/REVIEW.md` | Official Open-Meteo docs checked for `/v1/forecast`, `hourly=et0_fao_evapotranspiration`, UTC/time handling, and no key requirement. `cd Green-House\backend; python manage.py test api.tests.test_et0` -> 5 passed. `cd Green-House\backend; python manage.py test api` -> 26 passed. | Service is ready for task #003 but not wired into AMPC runtime yet, so ET0 fail-closed behavior is exposed as typed service output rather than persisted AMPC audit. Cache is Django application cache, not DB-backed across process restarts. Existing dirty/staged #001 and unrelated `views.py` changes remain preserved. | Implement #003 AMPC runtime wiring so unavailable ET0 persists pump-off recommendation/audit and queues no command. |
| 2026-05-12 | Codex | Re-reviewed Green-House task #001 after the non-finite/range validation fix. No blocking findings found: FAO soil presets still match the accepted plan, serializer now rejects `NaN`/`Infinity` and out-of-range coordinates, legacy `/api/auto-settings/` and greenhouse-scoped control-profile flows both persist/load the new physical config, and owner scoping remains covered. | `.claude/.claude/review/REVIEW.md`; reviewed `Green-House/backend/api/{models.py,serializers.py,views.py,ampc.py,migrations/0010_add_fao56_control_profile_config.py,tests/test_server_cutover.py}`, `Green-House/frontend/src/app/api/endpoints.ts`, `Green-House/.tasks/001-fao56-control-profile-config.md`. | Serializer probe rejected `inf`/`nan`/bad coordinates; targeted FAO API tests -> 4 passed; `cd Green-House\backend; python manage.py test api -v 1` -> 21 passed; `python manage.py check` -> OK; `python manage.py makemigrations --check --dry-run` -> no changes; frontend `npm test` -> smoke passed; `npm run build` -> OK; `python -m pytest Kalman\tests -q` -> 15 passed; `python -m pytest MPC\tests -q` -> 82 passed; `git diff --check` only CRLF warnings. | Frontend visible controls for FAO fields remain task #004; solver/runtime use of FAO fields remains later tasks. `views.py` still mixes pre-existing staged test-series changes with #001 mapping, so split carefully if committing. | Accept #001 or split/commit it, then proceed to Green-House #002 Open-Meteo ET0 service / MPC #011 water-balance model. |
| 2026-05-12 | Codex | Addressed Green-House task #001 review finding for non-finite FAO config. Added finite checks for every numeric FAO physical config field and coordinate bounds for latitude/longitude in `GreenhouseControlProfileSerializer`; added API regression tests proving `Infinity`, `NaN`, and out-of-range coordinates are rejected through both greenhouse-scoped and legacy auto-settings flows. | `Green-House/backend/api/serializers.py`, `Green-House/backend/api/tests/test_server_cutover.py`, `Green-House/.tasks/001-fao56-control-profile-config.md`, `.claude/.claude/review/REVIEW.md` | Targeted non-finite tests -> 2 passed; `cd Green-House\backend; python manage.py test api -v 1` -> 21 passed; `python manage.py check` -> OK; `python manage.py makemigrations --check --dry-run` -> no changes; `python -m py_compile api\serializers.py` -> OK; `git diff --check` only CRLF warnings. | Frontend visible controls for new FAO fields remain task #004. `views.py` still has pre-existing staged test-series changes plus unstaged #001 mapping, so split carefully if committing. | Re-review #001 after finite/range validation fix. |
| 2026-05-12 | Codex | Re-reviewed Green-House task #001 after preset fix using codex `bug-hunter`, `api-security-best-practices`, `backend-dev-guidelines`, and `architect-review`. Prior soil preset blocker is resolved and matches the accepted plan. New finding: DRF `FloatField` accepts `NaN`/`Infinity`, and current FAO serializer validation still lets non-finite values through for fields like `root_depth_m`, `pump_flow_lps`, `irrigation_area_m2`, and coordinates, so API can persist physically invalid config unless finite checks are added. | `.claude/.claude/review/REVIEW.md`; reviewed `Green-House/backend/api/{models.py,serializers.py,views.py,ampc.py,migrations/0010_add_fao56_control_profile_config.py,tests/test_server_cutover.py}`, `Green-House/frontend/src/app/api/endpoints.ts`, `Green-House/.tasks/001-fao56-control-profile-config.md`, `Green-House/TODO.md`. | `python manage.py test api -v 1` -> 19 passed; `python manage.py check` -> OK; `python manage.py makemigrations --check --dry-run` -> no changes; migration `py_compile` OK; frontend `npm test` -> smoke passed; `npm run build` -> OK; `python -m pytest Kalman\tests -q` -> 15 passed; `python -m pytest MPC\tests -q` -> 82 passed; `git diff --check` only LF/CRLF warnings. Serializer probe showed `root_depth_m=inf`, `pump_flow_lps=inf`, `irrigation_area_m2=nan`, `latitude=inf` validate as true. | Tests pass but do not cover non-finite float input. Existing staged `views.py` test-series hardening remains outside #001 and should be split if committing. | Add finite/range validation tests and reject non-finite FAO numeric inputs before accepting #001 as fully clean. |
| 2026-05-12 | Codex | Fixed Green-House task #001 review blocker. FAO soil presets now match the accepted plan exactly: `sand=0.10/0.04/0.45`, `light_loam=0.15/0.06/0.45`, `loam=0.32/0.15/0.45`, `clay_loam=0.35/0.23/0.45`; obsolete `sandy_loam` and `clay` choices were removed from model, migration, and tests. Confirmed the staged `views.py` Kalman/MPC test-series changes are outside task #001 and left them untouched. | `Green-House/backend/api/models.py`, `Green-House/backend/api/migrations/0010_add_fao56_control_profile_config.py`, `Green-House/backend/api/tests/test_server_cutover.py`, `Green-House/.tasks/001-fao56-control-profile-config.md`, `.claude/.claude/review/REVIEW.md` | Targeted FAO tests -> 4 passed; `cd Green-House\backend; python manage.py test api -v 1` -> 19 passed; `python manage.py check` -> OK; `python manage.py makemigrations --check --dry-run` -> no changes; migration `py_compile` -> OK; grep for obsolete preset names/values in #001 files returned no matches. | `views.py` still has pre-existing staged changes for Kalman/MPC test-series hardening plus unstaged #001 auto-settings mapping; split carefully if committing #001 separately. | Re-review #001 after preset correction, then proceed to #002 or split commits before committing. |
| 2026-05-12 | Codex | Reviewed Green-House task #001 implementation. Main finding: FAO soil presets in code/migration/tests drift from the accepted plan (`sand/light_loam/loam/clay_loam`, all `theta_sat=0.45`); current code uses `sand/sandy_loam/loam/clay_loam/clay` with different theta values, so tests pass against the wrong contract. Also noted unrelated staged `views.py` changes for Kalman/MPC test series that should not be bundled with #001 unless intentional. | `.claude/.claude/review/REVIEW.md`; reviewed `Green-House/backend/api/{models.py,serializers.py,views.py,ampc.py,migrations/0010_add_fao56_control_profile_config.py,tests/test_server_cutover.py}`, `Green-House/frontend/src/app/api/endpoints.ts`, `Green-House/.tasks/001-fao56-control-profile-config.md`, plan file. | Reran `cd Green-House\backend; python manage.py test api.tests.test_server_cutover -v 1` -> 19 passed; `python manage.py check` -> OK; `python manage.py makemigrations --check --dry-run` -> no changes. Review is not accepted due preset contract mismatch. | Tests currently encode the mismatched presets, so green tests do not prove domain correctness. | Fix presets/migration/tests to match the accepted FAO plan, then rerun backend gates and re-review #001 before accepting completion. |
| 2026-05-12 | Codex | Completed Green-House task #001: added per-greenhouse FAO-56 physical config to `GreenhouseControlProfile`, shared soil presets, serializer validation and preset population, legacy `/api/auto-settings/` mapping, AMPC config snapshots, frontend API type fields, additive Django migration, and regression tests for save/load, invalid theta ordering, and owner scoping. | `Green-House/backend/api/models.py`, `Green-House/backend/api/serializers.py`, `Green-House/backend/api/views.py`, `Green-House/backend/api/ampc.py`, `Green-House/backend/api/migrations/0010_add_fao56_control_profile_config.py`, `Green-House/backend/api/tests/test_server_cutover.py`, `Green-House/frontend/src/app/api/endpoints.ts`, `Green-House/TODO.md`, `Green-House/.tasks/001-fao56-control-profile-config.md`, `.claude/.claude/review/REVIEW.md` | `cd Green-House\backend; python manage.py test api -v 1` -> 19 passed; `python manage.py check` -> OK; `python manage.py makemigrations --check --dry-run` -> no changes; targeted FAO tests -> 4 passed; `python -m compileall -q api config` -> OK; migration `py_compile` -> OK; `cd Green-House\frontend; npm test` -> smoke passed; `npm run build` -> OK; `python -m pytest Kalman\tests -q` -> 15 passed; `python -m pytest MPC\tests -q` -> 82 passed. | `Green-House/docs/technical/CODEBASE_ONBOARDING.md` intentionally not updated until user review approval. Frontend visible controls for the new FAO fields remain task #004; solver/runtime use of FAO fields remains tasks #003/#006. Existing unrelated dirty edits in `views.py` and `test_server_cutover.py` were preserved. | Start Green-House #002 Open-Meteo ET0 service or MPC #011 FAO water-balance model next. |
| 2026-05-12 | Codex | Reviewed the FAO-56 AMPC plan and converted it into executable backlog tasks. Added MPC tasks #011-#012 for pure FAO water-balance contracts and Dr/TAW/RAW solver objective. Created Green-House task source (`TODO.md` + `.tasks/`) with tasks #001-#006 covering DB/API config, Open-Meteo ET0 cache/fail-closed service, AMPC runtime wiring, frontend settings, forecast audit UI, and final integration/docs gates. | `MPC/TODO.md`, `MPC/.tasks/011-fao56-water-balance-model.md`, `MPC/.tasks/012-fao56-dr-objective-recommendation.md`, `Green-House/TODO.md`, `Green-House/.tasks/*.md`, `Green-House/docs/plan/2026-05-12-fao-56-ampc-water-balance.md`, `.claude/.claude/review/REVIEW.md` | Preflight reread all 17 `.claude/.claude/rules/*.md`, root/MPC `CLAUDE.md`, review memory, Green-House and MPC `CODEBASE_ONBOARDING.md`, local project skills, and codex `plan-writing`/`task-intelligence`/`acceptance-orchestrator`. Verified TODO links resolve to task files, every new task has `required_skills` and 4 completion gates, and `git diff --check` only reports expected LF/CRLF warning. | Tasks are created but not implemented. Existing dirty runtime files remain unrelated: `Green-House/backend/api/views.py` and `Green-House/backend/api/tests/test_server_cutover.py`. | Start with MPC #011 and Green-House #001/#002 in parallel, then MPC #012, then Green-House #003-#006. |
| 2026-05-12 | Codex | Saved the user-confirmed FAO-56 AMPC water-balance plan: ET0 from Open-Meteo hourly FAO ET0, ETc = Kc*ET0, capacitive sensor percent mapped to theta via theta_wp/theta_sat with theta_sat=0.45, Dr/TAW/RAW/Ks formulas, pump irrigation depth, MPC stress/overwater cost, implementation phases, and tests. | `Green-House/docs/plan/2026-05-12-fao-56-ampc-water-balance.md`, `.claude/.claude/review/REVIEW.md` | Docs-only readback of the created plan file. No runtime tests run because no application code changed. | Plan is not implemented yet; schema/API/frontend/MPC/ET0 service/tests still need follow-up work. | Implement the FAO-56 AMPC plan in phases, starting with config schema/API and MPC water-balance unit tests. |
| 2026-05-12 | Codex | Traced owner question about the old FAO/low-high irrigation plan. Found the detailed FAO-oriented plan in git history: task #013 produced old `Server/docs/technical/AMPC_MODELING_HANDOFF.md` with θ soil moisture, FAO-56 `Dr`, `TAW`, θ↔Dr conversion, `ETc = Kc*ET0`, and starter water-balance equation. Current working tree only keeps a shortened `Green-House/docs/technical/AMPC_MODELING_HANDOFF.md`; current runtime still uses fixed `target_low=55`, `target_high=65`, and `crop_kc` is stored/exported but not used by MPC runtime. | `.claude/.claude/review/REVIEW.md`; inspected git history for `Server/docs/technical/AMPC_MODELING_HANDOFF.md`, `Kalman/.tasks/013...`, current `MPC/docs/technical/CONFIG.md`, `MPC/mpc/config.py`, `MPC/mpc/solver/cost.py`, `Green-House/backend/api/models.py`. | Git history lookup via `git log`/`git show`; no runtime tests run because this was documentation/source tracing only. | FAO conversion was documented/deferred, not implemented. Need soil/crop parameters and ET0 source before replacing fixed target band with FAO-derived thresholds. | If requested, create a new Green-House/MPC plan/task to restore FAO target-band derivation and wire it into `GreenhouseControlProfile` + controller config. |
| 2026-05-12 | Codex | Reviewed current repo structure/rules/source across `.claude`, `Green-House`, `Kalman`, `MPC`, `ARX`, and existing `.understand-anything` outputs. Findings to report: `.understand-anything` graph is stale versus current HEAD and still describes old Kalman backend/dashboard layers; `Green-House` runtime APIs still have several global queries/control paths despite `Greenhouse.owner`; live sensor dedupe omits greenhouse for runless readings; legacy/main scheduler state can still execute against first active greenhouse when `greenhouse_id` is null; several query params use raw `int()` and can 500 on bad input; `Green-House` has no TODO/task source of truth. | `.claude/.claude/review/REVIEW.md`; inspected source/docs/tests under `Green-House/`, `Kalman/`, `MPC/`, `ARX/`, `.claude/`, `.understand-anything/`. | `python -m pytest Kalman\tests -q` -> 15 passed; `python -m pytest MPC\tests -q` -> 82 passed; `cd Green-House\frontend; npm test` -> smoke passed; `npm run build` -> OK; `cd Green-House\backend; python manage.py check` -> OK; `python -m compileall -q Kalman\kalman MPC\mpc Green-House\backend\api Green-House\backend\config` -> OK. `python manage.py test api -v 1` blocked because local MySQL on `127.0.0.1:3306` refused connection. | Backend findings are from inspection plus available tests, not DB-backed Django test execution in this session. Worktree was already dirty/staged in `Green-House/backend/api/views.py` and `test_server_cutover.py`; no runtime files were modified. | Start MySQL/XAMPP and rerun `cd Green-House\backend; python manage.py test api -v 1`; then prioritize Green-House owner scoping + dedupe/scheduler hardening before accepting multi-user/runtime AMPC behavior. |
| 2026-05-09 | Codex | Fixed post-review split findings: `Server/backend/requirements.txt` no longer hardcodes editable `../../MPC` and `../../Kalman`; added `requirements-local.txt` for monorepo dev; added deploy artifact location `Server/backend/artifacts/arx_model.json` with settings fallback from Server-local artifact to monorepo ARX artifact; clarified `Kalman/` is algorithm-support, not strict filter-only; updated stale MPC README state-source wording to point at Server production integration. | `Server/backend/{requirements.txt,requirements-local.txt,artifacts/README.md,config/settings.py}`, `Server/README.md`, `Kalman/README.md`, `MPC/README.md`, `Server/.tasks/016-split-kalman-package-server.md`, `.claude/.claude/review/REVIEW.md` | `cd Server\backend; python manage.py check` -> OK; `python manage.py migrate --check` -> OK; `python -m pytest estimation\tests -q` -> 96 passed; `python -m pytest Kalman\tests -q` -> 13 passed; `python -m pytest MPC\tests -q` -> 80 passed; settings shell confirms monorepo fallback resolves to `ARX\arx_model.json` when Server artifact is absent. | `Server/docs/technical/CODEBASE_ONBOARDING.md` intentionally remains stale until owner review/acceptance. Standalone deploy still requires supplying `greenhouse-kalman` and `greenhouse-mpc` as wheels/private-index packages; source is not vendored into Server by design. | Owner review split/docs. If accepted, sync `Server/docs/technical/CODEBASE_ONBOARDING.md`; if strict filter-only is required, create a separate architecture task for shared contracts/generic filter API. |
| 2026-05-09 | Codex | Rechecked Server backend gates after owner started MySQL. The previously blocked MySQL validation now passes, so Server task #016 was marked completed in task/TODO tracking. | `Server/.tasks/016-split-kalman-package-server.md`, `Server/TODO.md`, `.claude/.claude/review/REVIEW.md` | `cd Server\backend; python manage.py migrate --check` -> OK; `python -m pytest estimation\tests -q` -> 96 passed. Prior same-task gates remain valid: Kalman tests 13 passed, dashboard tests 37 passed/build OK, MPC tests 80 passed. | `Server/docs/technical/CODEBASE_ONBOARDING.md` intentionally remains stale until owner review/acceptance of the split, per project rule. | Owner review split. If accepted, sync `Server/docs/technical/CODEBASE_ONBOARDING.md` to the new `Server/` + pure `Kalman/` boundary. |
| 2026-05-09 | Codex | Fixed task #015 post-review findings: unsafe/non-`safe` AMPC recommendations no longer call the actuator or persist `actuator_executed=True`; dashboard API client now supports DRF token auth, explicit Authorization headers, CSRF headers, and credentials options for authenticated AMPC/control-profile calls. | `Server/backend/estimation/control/service.py`, `Server/backend/estimation/tests/test_control_ampc.py`, `Server/dashboard/src/api/client.ts`, `Server/dashboard/src/__tests__/apiClient.test.ts`, `Server/dashboard/src/components/RunSelector.tsx`, `Server/docs/technical/{API,DATABASE}.md`, `Server/docs/user/USER_GUIDE.md`, `Kalman/.tasks/015-django-ampc-online-integration.md` | `python -m pytest estimation\tests\test_control_ampc.py -q` -> 28 passed; `python manage.py check` -> OK; `python -m pytest estimation\tests -q` -> 109 passed; dashboard `npm test -- --run` -> 37 passed; `npm run build` -> OK with existing Vite chunk-size warning. | AMPC POST idempotency/locking, dashboard read API auth defaults, and deeper actuator SSRF DNS/allowlist hardening remain earlier review risks not fixed in this targeted patch. `CODEBASE_ONBOARDING.md` intentionally remains unchanged until owner approval. | Re-review #015; if accepted, sync onboarding and decide whether to schedule idempotency/locking plus actuator allowlist as follow-up hardening. |
| 2026-05-09 | Codex | Reviewed Kalman task #014 and #015 after rereading root/project CLAUDE files, all rules, review memory, Kalman onboarding, TODO/task files, and applying codex `architect-review`, `bug-hunter`, `api-security-best-practices`, `backend-dev-guidelines`, and `acceptance-orchestrator`. Findings: AMPC POST lacks per-greenhouse idempotency/locking before actuator side effects; dashboard client cannot satisfy auth/CSRF/token requirements for POST/PATCH; dashboard read APIs can expose all runs when unauthenticated in dev/default mode; early fail-closed AMPC rows use `actuator_status=not_called` instead of documented `disabled`; actuator SSRF validation only blocks literal private/local hosts, not DNS resolution/allowlist. | `.claude/.claude/review/REVIEW.md`; reviewed `Server/backend/estimation/{models.py,migrations/0009_greenhouse_scope.py,migrations/0010_greenhousecontrolprofile_ampcrecommendation_and_more.py,control/*.py,api/{views.py,serializers.py,urls.py},tests/test_control_ampc.py,tests/test_api.py}`, dashboard API client/types/tests, task #014/#015 files. | `python manage.py check` -> OK; `python -m pytest estimation\tests\test_control_ampc.py estimation\tests\test_api.py -q` -> 33 passed. Review is code inspection plus targeted tests; no fixes applied in this prompt. | Current tests pass because they do not cover browser auth/CSRF, duplicate AMPC POST/concurrency, actuator disabled status on early fail-closed paths, or DNS-based SSRF. `CODEBASE_ONBOARDING.md` remains intentionally stale until owner review OK. | Fix #015 findings before accepting production AMPC; then rerun backend/MPC/dashboard gates and update onboarding after owner approval. |
| 2026-05-09 | Codex | Completed Kalman task #015: implemented Django AMPC online integration by authenticated `user + greenhouse_id`, with per-greenhouse control profiles, AMPC recommendation audit rows, DB-sourced latest Kalman state/history, server-side `settings.ARX_MODEL_PATH`, residual bias correction, optional fail-safe HTTP actuator branch, dashboard API client/types, ADR-006 and active docs. Fixed final review issues around solver/actuator exception fail-closed handling, profile numeric validation, and session/token auth compatibility. | `Server/backend/estimation/{models.py,migrations/0010_greenhousecontrolprofile_ampcrecommendation_and_more.py,control/,api/{serializers.py,views.py,urls.py},tests/test_control_ampc.py}`, `Server/backend/requirements.txt`, dashboard API client/types/tests, `Server/docs/technical/{API,ARCHITECTURE,DATABASE,DECISIONS}.md`, `Server/docs/user/USER_GUIDE.md`, `Kalman/TODO.md`, `Kalman/.tasks/015-django-ampc-online-integration.md`, `.understand-anything/` graph metadata. | `python manage.py makemigrations --check --dry-run` -> no changes; `python manage.py check` -> OK; `python manage.py migrate --check` -> OK; `python -m pytest estimation\tests -q` -> 108 passed; `python -m compileall -q estimation` OK; `python -m pytest MPC\tests -q` -> 80 passed; `python -m compileall -q MPC\mpc` OK; dashboard `npm test -- --run` -> 34 passed; `npm run build` OK with chunk-size warning only; AMPC smoke created recommendation row in rollback transaction; `git diff --check` only LF/CRLF warnings. | `CODEBASE_ONBOARDING.md` intentionally not updated until owner review OK. Real hardware actuator remains unpiloted and default off; operator must configure URL/env token outside API. `understand` skill file was unavailable, but previously built `.understand-anything` graph was used for targeted tracing. | Owner review #015. If accepted, update `Server/docs/technical/CODEBASE_ONBOARDING.md` to reflect greenhouse AMPC online integration and commit the completed task set if requested. |
| 2026-05-09 | Codex | Audited task #015 status after owner challenged missing completion/tick. Reread `.claude/.claude/rules/`, root/Kalman/MPC `CLAUDE.md`, review memory, `Server/docs/technical/CODEBASE_ONBOARDING.md`, `Kalman/TODO.md`, and task #015. Confirmed #015 is only partial implementation/in-progress, not completed: many backend/API/docs/test files are modified and AMPC control modules exist, but task checklist, completion gates, test gates, and self-review are not finished. | `.claude/.claude/review/REVIEW.md`; inspected `Kalman/TODO.md`, `Kalman/.tasks/015-django-ampc-online-integration.md`, `Server/backend/estimation/control/`, `Server/backend/estimation/{models.py,api,tests}`, dashboard API client/types, active docs, `.understand-anything/` graph outputs. | `git status --short`; `git diff --stat`; grep for #015 implementation surfaces; TODO/task grep confirms `status: in_progress`, `completed_at: null`, and no #015 completed checkbox. No runtime gates rerun in this audit prompt. | #015 must not be reported complete or ticked until implementation is resumed, defects fixed, validation gates pass, task checkboxes/gates are updated, and review memory records final evidence. `.understand-anything/` and `MPC/greenhouse_mpc.egg-info/` are untracked/generated and need cleanup/decision before final commit. | Resume #015 implementation immediately: run tests/checks, fix failures, complete docs/task checkboxes, perform skill-based self-review, then only tick TODO/task after all gates pass. |
| 2026-05-09 | Codex | Fixed task #014 process/documentation finding by adding explicit 4-gate completion evidence to the task file: Logic, Nghiệp vụ, Security, and Test chạy thực tế. No runtime code changed. | `Kalman/.tasks/014-add-greenhouse-ownership-scope.md`, `.claude/.claude/review/REVIEW.md` | Documentation-only verification: reviewed updated task file content and ran `git diff --check -- Kalman/.tasks/014-add-greenhouse-ownership-scope.md .claude/.claude/review/REVIEW.md` after edit; only LF/CRLF warning expected on Windows. | Runtime was not retested because this only records already-run gate evidence; prior #014 validation remains `81 passed` backend tests and clean migration/check gates. | If owner accepts #014, update `Server/docs/technical/CODEBASE_ONBOARDING.md` for greenhouse ownership before implementing #015. |
| 2026-05-09 | Codex | Reviewed Kalman task #014 after owner questioned checkbox reliability. Reread all `.claude/.claude/rules`, root/project `CLAUDE.md` files, review memory, `Server/docs/technical/CODEBASE_ONBOARDING.md`, TODO/task #014, and applied local/project review skills plus codex bug-hunter/backend-architect/api-security/database. Verified code actually implements greenhouse ownership scope rather than only ticking checkboxes. | `.claude/.claude/review/REVIEW.md`; reviewed `Server/backend/estimation/{models.py,migrations/0009_greenhouse_scope.py,api/{ingest.py,serializers.py,views.py},pipeline/store.py,run_config/service.py,tests/*.py}`, `Server/docs/technical/{API,ARCHITECTURE,DATABASE,DECISIONS}.md`, `Server/docs/user/USER_GUIDE.md`, `Kalman/.tasks/014-add-greenhouse-ownership-scope.md`, `Kalman/TODO.md` | `python manage.py check` OK; `python manage.py makemigrations --check --dry-run` no changes; `python manage.py migrate --check` OK; `showmigrations estimation` shows 0009 applied; DB verification `greenhouses=1`, `runs_missing_greenhouse=0`, `cycles_missing_greenhouse=0`, `cycles_total=105120`; `python -m pytest estimation/tests -q` -> 81 passed; targeted API/ingest/run_config/store tests -> 65 passed; `python -m compileall -q estimation` OK; `git diff --check` only LF/CRLF warnings. | No code/runtime findings found for #014. Process gap remains: task #014 file has skill/AC checkboxes but does not explicitly record the 4 completion gates in the task body; this review supplies the evidence, but future task files should encode those gates directly. `CODEBASE_ONBOARDING.md` remains intentionally stale until owner accepts #014. | If owner accepts #014, sync `CODEBASE_ONBOARDING.md`; otherwise fix the process gap in task #014 before moving to task #015 implementation. |
| 2026-05-09 | Codex | Converted the approved Django AMPC online integration plan into real Kalman backlog/task #015. Reread root/project CLAUDE files, rules, review memory, Kalman onboarding/TODO, existing Django models/API/settings/ingest code, and applied local planning/backend/database/quality/docs plus codex backend-architect/api-design/api-security/business-analyst/acceptance-orchestrator and agents understand for targeted code tracing. | `Kalman/TODO.md`, `Kalman/.tasks/015-django-ampc-online-integration.md`, `.claude/.claude/review/REVIEW.md` | Plan/task-only. Verified task file was created and TODO points to it. No runtime tests run because no application code changed. | `CODEBASE_ONBOARDING.md` intentionally remains stale until owner review OK. Task #015 is `todo`, not `in_progress`; implementation still needs schema/service/API/tests/docs and ADR/PRD sync to supersede old multi-greenhouse out-of-scope wording. | Start task #015 implementation after owner confirms the task breakdown, then run full backend/MPC validation gates. |
| 2026-05-09 | Codex | Wrote plan for Django AMPC online integration as a production multi-user/multi-greenhouse requirement: Django service/endpoint by `user + greenhouse_id`, server-side ARX artifact, DB-sourced latest Kalman state/history, greenhouse crop/controller profile, AMPC bias correction, recommendation persistence, dashboard JSON, optional actuator with fail-closed safety, and required test gates. Used local `planning`, `backend`, `database`, `quality`, `docs`, plus `backend-security-coder`; reread rules, CLAUDE files, review memory, Kalman/MPC onboarding, TODO, API/DB/ADR, and MPC runtime modules first. | `Server/docs/plan/2026-05-09-django-ampc-online-integration.md`, `.claude/.claude/review/REVIEW.md` | Plan-only; no runtime code or migrations changed. Verified plan file was created via `apply_patch`. | Plan intentionally does not update `CODEBASE_ONBOARDING.md`; implementation still needs task #015, schema migration, service/API/tests/docs. Current `Kalman/CLAUDE.md` old wording says multi-greenhouse out-of-scope in v1; plan calls for ADR-006 to supersede that for production. | On approval, create task #015 and implement the Django AMPC online integration from the plan. |
| 2026-05-09 | Codex | Completed Kalman task #014: added multi-user greenhouse ownership scope so live runs and prediction cycles store `greenhouse_id`, not duplicated `user_id`; migrated old `ExperimentRun.owner` data into per-owner default greenhouses; live ingest now authorizes via `Greenhouse.owner`; dashboard run API scopes authenticated users and supports `greenhouse_id` filter. | `Server/backend/estimation/{models.py,migrations/0009_greenhouse_scope.py,api/{ingest.py,serializers.py,views.py},pipeline/store.py,run_config/service.py,tests/*.py}`, `Server/docs/technical/{DATABASE,API,ARCHITECTURE,DECISIONS}.md`, `Server/docs/user/USER_GUIDE.md`, `Kalman/TODO.md`, `Kalman/.tasks/014-add-greenhouse-ownership-scope.md`, `.claude/.claude/review/REVIEW.md` | `python manage.py makemigrations --check --dry-run` -> no changes; `python manage.py check` -> OK; targeted backend tests -> 68 passed; full `python -m pytest estimation/tests -q` -> 81 passed; `python manage.py migrate` applied `estimation.0009_greenhouse_scope` OK; DB verification: `greenhouses=1`, `runs_missing_greenhouse=0`, `cycles_missing_greenhouse=0`; `showmigrations estimation` shows 0009 applied. | `CODEBASE_ONBOARDING.md` intentionally not updated until owner reviews/accepts this schema change; historical docs under `docs/Why` and older review docs still describe removed `ExperimentRun.owner`. Migration rollback cannot reconstruct old per-run owner column data except through greenhouse ownership. | Owner review #014; if accepted, sync `CODEBASE_ONBOARDING.md` and then wire AMPC/MPC online execution to read latest state by `greenhouse_id` with fixed `ARX/arx_model.json`. |
| 2026-05-09 | Codex | Completed MPC task #010: simplified CLI defaults so `recommend`, `simulate`, `adaptive-simulate`, and `auto` can run without explicit artifact/state/input/output paths; added `auto` alias for closed-loop; added `mpc.schema.default_config_schema()` and CLI `config-schema` for website-loadable defaults/user-vs-system field groups; implemented `MPC_CONFIG_PATH` fallback. | `MPC/mpc/{cli.py,config.py,schema.py,__init__.py}`, `MPC/tests/test_cli_simulation.py`, `MPC/docs/technical/{API,ARCHITECTURE,CONFIG,VALIDATION}.md`, `MPC/docs/user/USER_GUIDE.md`, `MPC/README.md`, `MPC/CLAUDE.md`, `MPC/CHANGELOG.md`, `MPC/TODO.md`, `MPC/.tasks/010-simplify-cli-defaults-config-schema.md`, `.claude/.claude/review/REVIEW.md` | `python -m pytest MPC\tests\test_cli_simulation.py MPC\tests\test_config.py -q` -> 39 passed; `python -m pytest MPC\tests -q` -> 80 passed; `python -m compileall -q MPC\mpc` OK; smoke from `MPC/`: `recommend --beam-width 4`, `simulate --max-steps 3 --beam-width 4`, `adaptive-simulate --max-steps 3 --beam-width 4`, `auto --beam-width 4`, and `config-schema --output reports\config_schema.json` all OK; smoke reports removed; `git diff --check` only LF/CRLF warnings. | `auto` currently aliases existing closed-loop service; real actuator still requires explicit config/env and AMPC live bias state integration remains future work beyond this CLI/default/schema task. `CODEBASE_ONBOARDING.md` not updated until owner review acceptance. | Owner review #010; if accepted, sync onboarding CLI/schema current state and plan live AMPC state-source integration. |
| 2026-05-09 | Codex | Completed MPC task #009: changed objective cost to normalize water/switching by `pump.max_seconds`, compute soft daily cap from total planned excess over `soft_daily_pump_cap_seconds`, require daily cap > 0, keep band/terminal squared error, and sync simulation report scoring/tests/docs. | `MPC/mpc/solver/cost.py`, `MPC/mpc/simulation/runner.py`, `MPC/mpc/config.py`, `MPC/tests/test_grid_solver.py`, `MPC/tests/test_config.py`, `MPC/docs/technical/{CONFIG,API,VALIDATION}.md`, `MPC/docs/user/USER_GUIDE.md`, `MPC/CHANGELOG.md`, `MPC/TODO.md`, `MPC/.tasks/009-normalize-mpc-objective-cost.md`, `.claude/.claude/review/REVIEW.md` | Targeted tests `python -m pytest MPC\tests\test_grid_solver.py MPC\tests\test_config.py MPC\tests\test_simulation_regression.py MPC\tests\test_cli_simulation.py -q` -> 52 passed; `python -m compileall -q MPC\mpc` OK; full `python -m pytest MPC\tests -q` -> 73 passed; CLI smoke `recommend`, `simulate`, `adaptive-simulate`, `closed-loop dry-run` OK; smoke reports removed. | Solver choices can change because water/switching penalties are larger when `pump.max_seconds < step_seconds`; weights may need tuning after real traces. `CODEBASE_ONBOARDING.md` not updated until owner review acceptance. | Review #009 behavior/output; if accepted, sync onboarding cost note if needed. |
| 2026-05-09 | Codex | Planned MPC cost-function correction requested by owner: normalize water/switching by `pump.max_seconds` instead of `step_seconds`, compute soft daily cap once from total planned horizon excess over daily cap, keep band and terminal band error squared. Read rules, CLAUDE files, onboarding, review memory, and local `planning`/`backend`/`quality` skills before planning. | `.claude/.claude/review/REVIEW.md` | Planning only; no runtime tests run. Inspected `MPC/mpc/solver/cost.py` and `MPC/tests/test_grid_solver.py` to identify change/test targets. | Needs implementation approval and docs/test updates because this changes objective-cost semantics and can alter solver choices. | Implement cost update in `MPC/mpc/solver/cost.py`, add regression tests, update CONFIG/API/VALIDATION docs, then run MPC test gates. |
| 2026-05-09 | Codex | Completed MPC task #008: documented runnable v2/v3 demo workflow and validation gates, added safe example state/dry-run config, synced `CODEBASE_ONBOARDING.md` module map through task #008, and moved TODO/task #008 to completed. Used local `docs` and `quality` after rereading rules, `.claude/CLAUDE.md`, `MPC/CLAUDE.md`, review memory, onboarding, TODO, task file, PRD, decisions, Q&A, and runtime docs/code. | `MPC/examples/{demo_state.json,closed_loop_dry_run.json}`, `MPC/docs/user/USER_GUIDE.md`, `MPC/docs/technical/{CODEBASE_ONBOARDING,VALIDATION}.md`, `MPC/README.md`, `MPC/CLAUDE.md`, `MPC/CHANGELOG.md`, `MPC/TODO.md`, `MPC/.tasks/008-demo-workflow-validation-gates.md`, `.claude/.claude/review/REVIEW.md` | `python -m pytest MPC\tests -q` -> 69 passed; `python -m compileall -q MPC\mpc` OK; CLI smoke from `MPC/`: `recommend` OK with top-level output/no envelope, `simulate` OK with `mpc,threshold`, `adaptive-simulate` OK with `ampc,mpc,threshold` and adaptive enabled, `closed-loop` dry check OK with `executed=false`, `status=config_error`, `pump_seconds=0.0`, `actuator_disabled`; generated smoke reports removed; `git diff --check` only LF/CRLF warnings. | Docs now assume task #007 is accepted because user requested task #008 after the #007 fix. Real actuator endpoint remains open and intentionally documented as operator-provided config. | Review task #008; next work should be a new MPC integration/demo task or backend integration task if needed. |
| 2026-05-09 | Codex | Fixed MPC task #007 review finding: `run_closed_loop()` now validates `config.actuator` via `HTTPActuatorClient.from_config()` before selecting an injected actuator client, so fake/DI clients cannot bypass explicit actuator guards. Added regression test for disabled default config plus injected client. | `MPC/mpc/closed_loop.py`, `MPC/tests/test_actuator_closed_loop.py`, `MPC/.tasks/007-v3-http-actuator-pilot.md`, `.claude/.claude/review/REVIEW.md` | `python -m pytest MPC\tests\test_actuator_closed_loop.py -q` -> 5 passed; `python -m compileall -q MPC\mpc` OK; `python -m pytest MPC\tests -q` -> 69 passed. | `CODEBASE_ONBOARDING.md` still intentionally waits for owner acceptance of task #007. Injected clients now require valid config/env even in tests. | Re-review #007; if accepted, sync onboarding and proceed to #008. |
| 2026-05-09 | Codex | Completed MPC task #007: added v3 HTTP actuator pilot with `ActuatorConfig`, `ActuatorCommand`, `HTTPActuatorClient`, `run_closed_loop()`, CLI `closed-loop`, fake HTTP actuator coverage, fail-closed handling for stale/config/HTTP errors, and docs/task/TODO sync. Used local `backend`, `quality`, `docs`, plus `backend-security-coder`; reread rules/CLAUDE/onboarding/review memory first. | `MPC/mpc/config.py`, `MPC/mpc/actuator/`, `MPC/mpc/closed_loop.py`, `MPC/mpc/cli.py`, `MPC/tests/test_actuator_closed_loop.py`, `MPC/tests/test_cli_simulation.py`, `MPC/tests/test_config.py`, `MPC/docs/technical/{API,ARCHITECTURE,CONFIG,VALIDATION}.md`, `MPC/docs/user/USER_GUIDE.md`, `MPC/README.md`, `MPC/CLAUDE.md`, `MPC/CHANGELOG.md`, `MPC/TODO.md`, `MPC/.tasks/007-v3-http-actuator-pilot.md`, `.claude/.claude/review/REVIEW.md` | `python -m pytest MPC\tests -q` -> 68 passed; `python -m compileall -q MPC\mpc` OK; real CLI smoke `closed-loop` with disabled actuator config wrote fail-closed output (`status=config_error`, `pump_seconds=0.0`, `actuator_disabled`) and no HTTP; TODO/task #007 checkbox sync verified; `git diff --check` only LF/CRLF warnings. | `CODEBASE_ONBOARDING.md` intentionally not advanced to task #007 until owner review approval; closed-loop pilot has no retry and no real hardware test by design, only fake HTTP. | Review task #007; if accepted, sync `CODEBASE_ONBOARDING.md` and proceed to #008 demo workflow/validation docs. |
| 2026-05-09 | Codex | Fixed MPC task #006 review findings: `run_adaptive_simulation()` now enables adaptive bias internally even when caller passes default static `ControllerConfig()`, added regression coverage for direct API default config, and synced MPC onboarding with task #006/current validation context after owner review. | `MPC/mpc/simulation/runner.py`, `MPC/tests/test_simulation_regression.py`, `MPC/docs/technical/CODEBASE_ONBOARDING.md`, `MPC/CHANGELOG.md`, `MPC/.tasks/006-v3-bias-adaptation-layer.md`, `.claude/.claude/review/REVIEW.md` | `python -m pytest MPC\tests -q` -> 58 passed; `python -m compileall -q MPC\mpc` OK; real `adaptive-simulate` smoke with `../ARX/arx_model.json` and `--max-steps 3` OK, generated report removed. | `adaptive-simulate` still uses moving-average bias only, not RLS/model update by design. | Re-review #006 fixes; if accepted, start #007 closed-loop HTTP actuator pilot with fail-safe. |
| 2026-05-09 | Codex | Completed MPC task #006: added v3 AMPC bias adaptation with guarded residual updates, bias-corrected horizon forecasts, adaptive simulation report comparing `mpc`/`ampc`/`threshold`, CLI `adaptive-simulate`, observation-error metrics, docs, and regression tests. Used local `backend`, `quality`, `docs`, plus system `backend-security-coder` after rereading rules/CLAUDE/onboarding/review memory. | `MPC/mpc/config.py`, `MPC/mpc/adaptive/`, `MPC/mpc/simulation/`, `MPC/mpc/cli.py`, `MPC/tests/`, `MPC/docs/technical/{API,ARCHITECTURE,CONFIG,VALIDATION}.md`, `MPC/README.md`, `MPC/CLAUDE.md`, `MPC/CHANGELOG.md`, `MPC/TODO.md`, `MPC/.tasks/006-v3-bias-adaptation-layer.md`, `.claude/.claude/review/REVIEW.md` | `python -m pytest MPC\tests -q` -> 57 passed; `python -m compileall -q MPC\mpc` OK; real CLI smoke `python -m mpc adaptive-simulate --artifact ..\ARX\arx_model.json --input ..\ARX\greenhouse_data.csv --output reports\v3_adaptive_smoke.json --max-steps 3 --beam-width 4` OK, generated smoke report removed; TODO/task #006 checkbox sync verified; `git diff --check` only LF/CRLF warnings. | `CODEBASE_ONBOARDING.md` intentionally not updated for task #006 until owner review approval; adaptive bias is moving-average correction only, not RLS/model-update. | Review task #006; if accepted, sync `CODEBASE_ONBOARDING.md` before task #007 closed-loop actuator pilot. |
| 2026-05-09 | Codex | Fixed MPC task #005 review findings: config JSON integer fields now reject floats/strings/bools instead of truncating, task #005 metadata includes `docs`, and README/CLAUDE current status is synced to task #005. | `MPC/mpc/config.py`, `MPC/tests/test_config.py`, `MPC/.tasks/005-v2-test-suite.md`, `MPC/README.md`, `MPC/CLAUDE.md`, `.claude/.claude/review/REVIEW.md` | `python -m pytest MPC\tests -q` -> 48 passed; `python -m compileall -q MPC\mpc` OK; CLI smoke with `step_seconds=300.9` exits 2 and writes no output. | `CODEBASE_ONBOARDING.md` still intentionally waits for owner review acceptance after task #005. | Re-review #005 fixes; if accepted, sync onboarding/read-order with `VALIDATION.md` before task #006. |
| 2026-05-09 | Codex | Completed MPC task #005: expanded v2 QA coverage with config validation/load tests, CLI error smoke tests, simulation regression metrics, and validation gate documentation. Used local `quality` skill after rereading required rules/CLAUDE/onboarding/review memory. | `MPC/tests/test_config.py`, `MPC/tests/test_simulation_regression.py`, `MPC/tests/test_cli_simulation.py`, `MPC/docs/technical/VALIDATION.md`, `MPC/README.md`, `MPC/CLAUDE.md`, `MPC/CHANGELOG.md`, `MPC/TODO.md`, `MPC/.tasks/005-v2-test-suite.md`, `.claude/.claude/review/REVIEW.md` | `python -m pytest MPC\tests -q` -> 43 passed; `python -m compileall -q MPC\mpc` OK; TODO/task #005 status grep clean; `git diff --check` only LF/CRLF warnings. | `CODEBASE_ONBOARDING.md` intentionally not updated for task #005 until owner review acceptance; full CSV simulation remains potentially slow without `--max-steps`. | Review #005; if accepted, sync onboarding/read-order with `VALIDATION.md` before task #006. |
| 2026-05-09 | Codex | Fixed MPC task #004 review findings: `recommend` CLI output now matches the public top-level recommendation contract, simulation report objective cost resets soft daily cap per calendar day, and `CODEBASE_ONBOARDING.md` is synced after owner review. Added regression coverage for top-level CLI output and multi-day daily cap cost. | `MPC/mpc/cli.py`, `MPC/mpc/simulation/runner.py`, `MPC/tests/test_cli_simulation.py`, `MPC/docs/technical/CODEBASE_ONBOARDING.md`, `MPC/CHANGELOG.md`, `MPC/.tasks/004-v2-simulation-cli-baseline-report.md`, `.claude/.claude/review/REVIEW.md` | `python -m pytest MPC\tests -q` -> 31 passed; `python -m compileall -q MPC\mpc` OK; real CLI smoke verified `recommend` has top-level `pump_seconds` and no `recommendation`/`config` envelope; real `simulate` smoke OK; stale onboarding grep clean; `git diff --check` only LF/CRLF warning for review memory. | Full CSV simulation without `--max-steps` can still be slow by design; task #005 should broaden regression coverage. | Re-review #004 fixes, then start task #005. |
| 2026-05-09 | Codex | Completed MPC task #004: added standalone `python -m mpc simulate` and `python -m mpc recommend`, threshold baseline comparison, simulation report metrics, CLI JSON/config loading with UTF-8 BOM tolerance, smoke tests, and user/API docs. Used local `quality` and `docs` skills after rereading project rules/CLAUDE/onboarding/review memory. | `MPC/mpc/config.py`, `MPC/mpc/cli.py`, `MPC/mpc/__main__.py`, `MPC/mpc/simulation/`, `MPC/mpc/__init__.py`, `MPC/pyproject.toml`, `MPC/tests/test_cli_simulation.py`, `MPC/README.md`, `MPC/CHANGELOG.md`, `MPC/docs/user/USER_GUIDE.md`, `MPC/docs/technical/API.md`, `MPC/docs/technical/ARCHITECTURE.md`, `MPC/CLAUDE.md`, `MPC/TODO.md`, `MPC/.tasks/004-v2-simulation-cli-baseline-report.md`, `.claude/.claude/review/REVIEW.md` | `python -m pytest MPC\tests -q` -> 30 passed; `python -m compileall -q MPC\mpc` OK; real CLI smoke for `simulate` with `../ARX/arx_model.json` and `--max-steps 3` OK; real CLI smoke for `recommend` OK after BOM-tolerant JSON read; stale wording/TODO #004 grep clean; `git diff --check` only LF/CRLF warning for review memory. | Full `../ARX/greenhouse_data.csv` simulation without `--max-steps` may be slow because it solves MPC at every row; docs recommend `--max-steps 288` for quick baseline report. `CODEBASE_ONBOARDING.md` intentionally not updated until owner review approval. | Review task #004; if accepted, update `CODEBASE_ONBOARDING.md` before starting task #005. |
| 2026-05-09 | Codex | Cleaned post-task #003 README stale wording: README now states solver/recommendation already exists from #003 and CLI/simulation remains #004. | `MPC/README.md`, `.claude/.claude/review/REVIEW.md` | `rg` confirmed stale `task #003/#004` and `solver/recommendation tiếp tục` wording is gone from README. | Docs-only change; runtime behavior unchanged. | Proceed to task #004 after review acceptance. |
| 2026-05-08 | Codex | Fixed MPC task #003 review findings: solver now normalizes forecast history by replacing latest record with `ControllerState`, rejects future timestamps beyond 30s clock skew, documents beam-grid solver approximation, and syncs preflight docs after task #003. | `MPC/mpc/solver/grid.py`, `MPC/tests/test_grid_solver.py`, `MPC/docs/technical/API.md`, `MPC/docs/technical/CODEBASE_ONBOARDING.md`, `MPC/CLAUDE.md`, `MPC/README.md`, `MPC/.tasks/003-v2-grid-shooting-solver.md`, `.claude/.claude/review/REVIEW.md` | `python -m pytest MPC/tests -q` -> 26 passed; `python -m compileall -q MPC/mpc` OK. | Beam-grid remains approximate for full horizon by design; exhaustive equivalence is only promised for small horizons or sufficiently large beam width. | Re-review #003; if accepted, start #004 simulation CLI. |
| 2026-05-08 | Codex | Completed MPC task #003: implemented deterministic grid-shooting beam solver, cost scoring, public `Recommendation` output contract, pump grid candidates, and fail-closed handling for stale/invalid/model-error inputs. Used `backend`, `quality`, and `backend-security-coder` skills. | `MPC/pyproject.toml`, `MPC/mpc/__init__.py`, `MPC/mpc/config.py`, `MPC/mpc/types.py`, `MPC/mpc/solver/{__init__.py,cost.py,grid.py}`, `MPC/tests/test_grid_solver.py`, `MPC/README.md`, `MPC/CLAUDE.md`, `MPC/TODO.md`, `MPC/.tasks/003-v2-grid-shooting-solver.md`, `.claude/.claude/review/REVIEW.md` | `python -m pytest MPC/tests -q` -> 24 passed; `python -m compileall -q MPC/mpc` OK. | CLI/simulation are still pending for task #004; `MPC/docs/technical/CODEBASE_ONBOARDING.md` intentionally not updated after solver code until owner reviews task #003. | Start task #004: simulation CLI and baseline metrics report using the solver contract. |
| 2026-05-08 | Codex | Fixed MPC pre-#003 review findings: updated onboarding current state/validation gate after task #002 runtime package, added `backend-security-coder` to task #003 required skills, and marked the old task #002 ARX lag history row as superseded. | `MPC/docs/technical/CODEBASE_ONBOARDING.md`, `MPC/.tasks/003-v2-grid-shooting-solver.md`, `MPC/.tasks/002-arx-plant-adapter-state-history.md`, `.claude/.claude/review/REVIEW.md` | Grep confirmed no stale "chưa có package runtime" / old package-runtime gate text; task #003 metadata now includes `backend-security-coder`; `git diff --check` only reports existing LF/CRLF warning for review memory. | This was docs/task metadata only; no runtime tests rerun. | Before task #003, read `backend`, `quality`, and `backend-security-coder` skills. |
| 2026-05-08 | Codex | Corrected MPC ARX lag review fix: for one-step forecast, input regressors are indexed from augmented `[*history, decision_record]`, so `lag 1` is current decision and `lag 2` is `history[-1]`. Updated regression expectation accordingly. | `MPC/mpc/plant/arx.py`, `MPC/tests/test_arx_plant.py`, `MPC/.tasks/002-arx-plant-adapter-state-history.md`, `.claude/.claude/review/REVIEW.md` | `python -m pytest MPC/tests -q` -> 13 passed; `python -m compileall -q MPC/mpc` OK. | Prior review-memory row mentions the superseded `history[-lag]` fix; latest row is authoritative. | Continue task #002 review or move to #003 after acceptance. |
| 2026-05-08 | Codex | Fixed MPC task #002 review findings: corrected ARX `nb > 1` input lag indexing so lag 1 uses current decision and lag > 1 uses `history[-lag]`; tightened `ControllerState.from_mapping()` validation for `last_pump_seconds=None` and non-int `run_id`. Added regression tests. | `MPC/mpc/plant/arx.py`, `MPC/mpc/state.py`, `MPC/tests/test_arx_plant.py`, `MPC/tests/test_state.py`, `MPC/.tasks/002-arx-plant-adapter-state-history.md`, `.claude/.claude/review/REVIEW.md` | `python -m pytest MPC/tests -q` -> 13 passed; `python -m compileall -q MPC/mpc` OK. | `MPC/docs/technical/CODEBASE_ONBOARDING.md` still intentionally not updated until user accepts task #002. | Re-review task #002, then proceed to #003 after acceptance. |
| 2026-05-08 | Codex | Completed MPC task #002: created importable `mpc` package with config dataclasses, state/history contracts, measured-hold disturbance forecast, and ARX plant adapter that loads `../ARX/arx_model.json` without training. Used local `backend` and `quality` skills; corrected ARX forecast ordering so output lags come from history while input lag 1 uses the current pump decision. | `MPC/pyproject.toml`, `MPC/mpc/__init__.py`, `MPC/mpc/config.py`, `MPC/mpc/state.py`, `MPC/mpc/plant/{__init__.py,base.py,arx.py}`, `MPC/tests/{conftest.py,test_state.py,test_arx_plant.py}`, `MPC/README.md`, `MPC/CLAUDE.md`, `MPC/TODO.md`, `MPC/.tasks/002-arx-plant-adapter-state-history.md`, `.claude/.claude/review/REVIEW.md` | `python -m pytest MPC/tests -q` -> 10 passed; `python -m compileall -q MPC/mpc` OK; generated `__pycache__` directories removed after verification. | Solver/CLI are still pending for task #003/#004; `MPC/docs/technical/CODEBASE_ONBOARDING.md` intentionally not updated until user reviews task #002. | Start task #003: implement v2 grid-shooting MPC recommendation solver using the task #002 plant/state contracts. |
| 2026-04-14 | Codex | Sửa doc mismatch còn sót trong task #002: `DATABASE.md` phần Migration Notes không còn ghi `sample_ts` là `DATETIME(3)`, thống nhất với model/migration hiện tại là Django `DateTimeField` -> MySQL `DATETIME` không explicit millisecond precision. | `Server/docs/technical/DATABASE.md`, `.claude/.claude/review/REVIEW.md` | Verify `rg --fixed-strings "DATETIME(3)" Server/docs/technical/DATABASE.md` không còn match; Django verification trước đó đã pass: `check`, `makemigrations --check --dry-run`, `migrate --check`, `showmigrations estimation [X]`. | Nếu sau này cần millisecond precision thật, phải thêm custom DDL migration riêng thay vì chỉ sửa doc. | Task #002 sạch cả schema, migration, DB local và docs; có thể bắt đầu #003. |
| 2026-04-13 | Codex | Tạo bản dịch tiếng Việt cho `README.md` trong thư mục `.claude/.Vn`; giữ nguyên file gốc để đối chiếu. | `.claude/.Vn/README.md`, `.claude/.claude/review/REVIEW.md` | `README.md` bản dịch đọc OK bằng UTF-8; đã rà heading chính bằng `Select-String`; `.claude/.Vn` hiện có 4 file dịch. | Một số thuật ngữ kỹ thuật như plugin, agent, hook, skill, task được giữ nguyên để khớp hệ sinh thái Claude. | Nếu cần biến bản dịch thành README chính, cần xác nhận trước khi thay file gốc. |
| 2026-04-13 | Codex | Tạo bản dịch tiếng Việt cho `PRD.md`, `SOUL.md`, `START_HERE.md` trong thư mục `.claude/.Vn`; không sửa file gốc. | `.claude/.Vn/PRD.md`, `.claude/.Vn/SOUL.md`, `.claude/.Vn/START_HERE.md`, `.claude/.claude/review/REVIEW.md` | 3 file dịch đọc OK bằng UTF-8; đã rà heading chính bằng `Select-String`. | `SOULD` trong prompt được hiểu là `SOUL.md` vì file gốc tồn tại là `SOUL.md`. | Nếu cần dùng bản dịch làm nguồn chính, cần xác nhận có thay thế hay chỉ giữ bản đối chiếu trong `.Vn`. |
| 2026-04-13 | Codex | Thêm workflow review memory, plan trước task, discussion trước phê duyệt, và rule đọc context trước khi làm việc. | `.claude/.plans/README.md`, `.claude/.discuss/README.md`, `.claude/.claude/review/REVIEW.md`, `.claude/.claude/rules/*.md`, `.claude/CLAUDE.md`, `.claude/.claude/settings.json` | `settings.json` parse OK; 9 file đã chạm đọc OK bằng UTF-8; không còn mẫu tiếng Việt không dấu mục tiêu trong vùng đã sửa. | Hook `UserPromptSubmit` phụ thuộc vào runtime Claude Code hỗ trợ event này; rule trong `CLAUDE.md` vẫn là ràng buộc chính. | Đọc lại review và rules trước mỗi prompt tiếp theo. |
| 2026-04-13 | Codex | Gỡ workflow bắt buộc plan trước task; chuyển nguồn task chính về `.claude/TODO.md` và `.tasks/`. | `.claude/CLAUDE.md`, `.claude/.claude/rules/task-skill-gates.md`, `.claude/.claude/rules/preflight-check.md`, `.claude/.claude/settings.json`, `.claude/.discuss/README.md`, `.claude/.claude/rules/planning-approval.md`, `.claude/.plans/` | `.claude/.plans` đã bị xóa; `planning-approval.md` không còn; `settings.json` parse OK; đã rà reference `.plans`/plan-gate trong `.claude` ngoài lịch sử cũ. | Một số dòng lịch sử cũ vẫn nhắc workflow plan để giữ tính append-only của review memory. | Tiếp tục dùng `.claude/TODO.md` và `.claude/.tasks/` khi tạo/cập nhật task; chỉ tạo plan/discussion nếu user yêu cầu rõ. |
| 2026-04-13 | Codex | Xóa tiếp `.claude/.discuss` và gỡ discussion khỏi rule/entrypoint vì chỉ cần TODO + tasks. | `.claude/.discuss/`, `.claude/CLAUDE.md`, `.claude/.claude/rules/preflight-check.md`, `.claude/.claude/rules/task-skill-gates.md`, `.claude/.claude/review/REVIEW.md` | Đã rà `.discuss`/discussion reference ngoài lịch sử review; `.claude/.discuss` không còn. | Lịch sử cũ trong review vẫn nhắc `.discuss` để giữ append-only. | Dùng `.claude/TODO.md` và `.claude/.tasks/` cho task tracking. |
| 2026-04-13 | Codex | Chạy `/start` theo `.claude/START_HERE.md`; đã đọc startup docs, rules, review memory, TODO và task index. | `.claude/START_HERE.md`, `.claude/.claude/commands/start.md`, `.claude/CLAUDE.md`, `.claude/.claude/rules/*.md`, `.claude/TODO.md`, `.claude/.tasks/`, `.claude/.claude/review/REVIEW.md` | Xác nhận onboarding đang ở Phase 1 Group 1; chưa sửa docs/backlog vì cần user trả lời trước. | Các file template/backlog vẫn còn placeholder; cần thu thập thông tin dự án trước khi cập nhật. | Hỏi user Group 1: project basics. |
| 2026-04-13 | Codex | Ghi nhận câu trả lời Phase 1 Group 1 cho onboarding: project `HMPC`, smart greenhouse dùng sensor realtime, Adaptive Kalman filtering và HMPC để tối ưu irrigation/misting/ventilation; primary user là chính user; problem là greenhouse control thủ công/threshold-based bị reactive, chậm, lãng phí nước/năng lượng và làm điều kiện trồng thiếu ổn định. | `.claude/.claude/review/REVIEW.md` | Đã đối chiếu Group 2 trong `.claude/START_HERE.md`; chưa cập nhật docs vì còn thiếu stack/conventions/features. | Cần tiếp tục thu thập Phase 1 Group 2-6 trước khi thay placeholder docs/backlog. | Hỏi user Group 2: tech stack. |
| 2026-04-13 | Codex | Ghi nhận câu trả lời Phase 1 Group 2: frontend Vite, backend Python, database MySQL từ XAMPP, query layer Django ORM, deployment AWS, package manager npm, Node latest/not pinned. | `.claude/.claude/review/REVIEW.md` | Quét repo bằng `rg --files` chỉ thấy `ARX/requirements.txt`; không thấy `.nvmrc` hoặc `package.json`, nên Node version chưa được pin trong repo. | `Py` được hiểu là Python; cần xác nhận nếu backend không phải Django/Python đầy đủ. | Hỏi user Group 3: formatter/linter, test runner, dev/build/test commands. |
| 2026-04-13 | Codex | Ghi nhận câu trả lời Phase 1 Group 3: formatter/linter là Prettier + ESLint, unit test runner là Vitest, commands là `npm run dev`, `npm run build`, `npm test`, `npm start`. | `.claude/.claude/review/REVIEW.md` | Đã đối chiếu Group 4 trong `.claude/START_HERE.md`; chưa cập nhật docs vì còn thiếu yêu cầu v1, content/SEO và mục tiêu. | Backend Python/Django chưa có commands riêng như `python manage.py runserver` hoặc `pytest`; tạm ghi nhận commands user cung cấp là npm commands. | Hỏi user Group 4: product requirements. |
| 2026-04-13 | Codex | Ghi nhận Phase 1 Group 4: v1 là technical validation release cho pipeline `Sensor/Dataset -> Preprocess -> ARX Predict -> Kalman Update -> Store -> Visualize -> Evaluate`; dùng `ARX/greenhouse_data.csv`; tính năng gồm live/batch ingestion, validation/preprocessing, reuse ARX prediction block, stable Kalman cycle, compare raw/predicted/filtered, storage/logging, experiment config, evaluation metrics/export. NFR: ARX+Kalman <=500ms, dashboard <=5s, uptime demo >=95%, sample loss <2%/day, authorized config, raw/ARX/Kalman traceability. Out of scope: HMPC/full optimal control, autonomous actuation, advanced adaptive Kalman, multi-greenhouse/zone/tenant, cloud-scale ops, native mobile, weather/ET0/physical models, automatic retraining/model registry. Product owner: user. | `.claude/.claude/review/REVIEW.md` | Nội dung Group 4 đủ để tạo FR/NFR/out-of-scope trong PRD; chưa cập nhật docs cho tới khi hoàn tất Group 5-6. | Cần xác định có public-facing content/SEO không; nếu internal/demo tool thì CONTENT_STRATEGY sẽ được đánh dấu N/A. | Hỏi user Group 5: content/SEO hoặc xác nhận internal tool. |
| 2026-04-13 | Codex | Ghi nhận Phase 1 Group 5: có public-facing website/landing/marketing sau này nhưng chưa có trong v1; chưa có brand voice, SEO goals, hay page copy. | `.claude/.claude/review/REVIEW.md` | Đã đối chiếu Group 6 trong `.claude/START_HERE.md`; còn thiếu success criteria và open decisions. | CONTENT_STRATEGY nên ghi public-facing future scope, v1 chưa có nội dung/SEO; nhiều mục để `[TBD]`. | Hỏi user Group 6: success metrics và open decisions. |
| 2026-04-13 | Codex | Ghi nhận Phase 1 Group 6: v1 success là pipeline `data input -> ARX prediction -> Kalman update -> storage -> visualization -> evaluation` chạy đúng/ổn định, giảm nhiễu so với raw data, có logging/visualization/evidence, academically defendable và extensible lên adaptive estimation/HMPC. Metrics: >=95% cycle success, <=500ms cycle latency, <=5s output update delay, <2% sample loss over 24h, 20-30% variance reduction, bounded residuals, raw/predicted/filtered charts, complete per-cycle logging, no crash on short missing/noisy data, reproducible runs from `ARX/greenhouse_data.csv` and saved config. Open decisions: estimation variables, ARX integration mode, CSV usage, Kalman variant/parameters, offline-first vs real-time-first, visualization scope, storage format, good-enough performance threshold, formal v2 scope. | `.claude/.claude/review/REVIEW.md` | Phase 1 complete; enough information to fill PRD/docs and derive real tasks. | Some open decisions intentionally remain `[TBD]` and should be tracked in PRD/backlog. | Start Phase 2/3: copy templates, fill docs, create TODO and task files. |
| 2026-04-13 | Codex | Hoàn tất Phase 2/3 `/start` ở project root: tạo README/CLAUDE/PRD/TODO, copy và chỉnh docs, tạo 12 task thật từ PRD v1; giữ `.claude/START_HERE.md` vì đang chờ user xác nhận Phase 4. | `README.md`, `CLAUDE.md`, `PRD.md`, `TODO.md`, `docs/technical/ARCHITECTURE.md`, `docs/technical/DECISIONS.md`, `docs/technical/API.md`, `docs/technical/DATABASE.md`, `docs/technical/DESIGN_SYSTEM.md`, `docs/user/USER_GUIDE.md`, `docs/content/CONTENT_STRATEGY.md`, `.tasks/000-initial-project-setup.md`, `.tasks/001-*.md` đến `.tasks/012-*.md`, `.tasks/TASK_TEMPLATE.md`, `.claude/.claude/review/REVIEW.md` | Structural checks pass: all required docs exist; every TODO id #000-#012 maps to a `.tasks` file; no original placeholder markers found in required filled docs. App tests not run because this was docs/backlog work and root `package.json` does not exist yet. | `.claude/START_HERE.md` chưa xóa; plugin symlink chưa tạo vì Phase 5 cần user xác nhận trước. Một số quyết định v1 vẫn là TBD/open trong PRD. | Present Phase 4 summary and ask user to confirm or request corrections. |
| 2026-04-13 | Codex | Thêm rule mặc định báo cáo/câu trả lời cho user bằng tiếng Việt có dấu theo yêu cầu user và chuẩn bị báo cáo lại onboarding bằng tiếng Việt. | `.claude/.claude/rules/language.md`, `CLAUDE.md`, `.claude/.claude/review/REVIEW.md` | `Select-String` xác nhận rule tiếng Việt tồn tại ở cả hai file; kiểm tra lại required docs đều tồn tại; TODO #000-#012 đều có `.tasks` tương ứng. | Một số docs nội dung đã tạo trước đó vẫn viết bằng tiếng Anh; rule mới áp dụng cho báo cáo/câu trả lời và các cập nhật tiếp theo trừ khi user yêu cầu khác. | Báo cáo lại Phase 4 bằng tiếng Việt và chờ user xác nhận để chạy Phase 5. |
| 2026-04-13 | Codex | Di chuyển artifact dự án từ root vào `Kalman/`; cập nhật rules/entrypoint để dùng `Kalman/` làm project target, `ARX/` làm context đọc tham khảo, `.claude/` làm quản lý agent/template; cập nhật tham chiếu dataset thành `../ARX/...`. | `Kalman/README.md`, `Kalman/CLAUDE.md`, `Kalman/PRD.md`, `Kalman/TODO.md`, `Server/docs/`, `Kalman/.tasks/`, `.claude/CLAUDE.md`, `.claude/START_HERE.md`, `.claude/.claude/rules/*.md`, `.claude/.claude/review/REVIEW.md` | Root listing chỉ còn `.claude`, `ARX`, `Kalman`; `Kalman` có `.tasks`, `docs`, `CLAUDE.md`, `PRD.md`, `README.md`, `TODO.md`; TODO #000-#012 đều có task file; `rg --pcre2` không còn legacy root-path patterns trong active project/rule files, bỏ qua template generic và review history append-only. | Repo này không có `.git`, nên không chạy được `git status`; app tests chưa chạy vì chỉ tổ chức docs/path và chưa có package/project code trong `Kalman/`. | Tiếp tục code/doc trong `Kalman/`; đọc `../ARX/greenhouse_data.csv` làm nguồn dữ liệu tham khảo khi triển khai v1. |
| 2026-04-14 | Codex | Kiểm tra lại trạng thái `START_HERE.md` theo yêu cầu user; dọn backlog/task placeholder cũ trong `.claude` để task source của dự án chỉ còn trong `Kalman/`. | `.claude/TODO.md`, `.claude/.tasks/`, `.claude/.claude/review/REVIEW.md` | Root listing chỉ còn `.claude`, `ARX`, `Kalman`; `.claude/TODO.md` và `.claude/.tasks` không còn; `Kalman/TODO.md`, `Kalman/.tasks`, `Kalman/README.md`, `Kalman/PRD.md`, `Kalman/CLAUDE.md`, `Server/docs` đều tồn tại; TODO #000-#012 đều map tới task file; 13 task file đều có metadata chính và các section Description/Acceptance Criteria; active path scan pass. | Phase 5 chưa hoàn tất vì `.claude/START_HERE.md` vẫn còn và rule trong file yêu cầu chỉ xóa sau khi user xác nhận hài lòng; app tests không chạy vì đây là kiểm tra/tổ chức docs và chưa có package/project code. | Báo cáo cho user: Phase 2-4 OK, Phase 5 chờ xác nhận xoá `START_HERE.md`. |
| 2026-04-14 | Codex | Trả lời thắc mắc của user về 24 câu Phase 1; tạo file lưu Q/A raw để tránh mất ngữ cảnh và thêm vào docs agent cần đọc. | `Server/docs/technical/ONBOARDING_ANSWERS.md`, `Kalman/CLAUDE.md`, `.claude/CLAUDE.md`, `.claude/.claude/review/REVIEW.md` | Đọc lại `START_HERE.md`, `PRD.md`, `README.md`, `CLAUDE.md`; xác nhận file Q/A mới tồn tại, `Kalman/CLAUDE.md` và `.claude/CLAUDE.md` đều trỏ tới `ONBOARDING_ANSWERS.md`; active path scan pass. | Một số câu trả lời vẫn có open decisions vì chính user đã đánh dấu chưa chốt; cần giải quyết trong task #001 trước khi implementation khóa thiết kế. | Khi cần hiểu yêu cầu gốc, đọc `Server/docs/technical/ONBOARDING_ANSWERS.md` trước rồi đối chiếu `PRD.md` và `TODO.md`. |
| 2026-04-14 | Codex | Read `BaoCao.md`, `Tonghop.md`, `Tonghop2.md`; updated project direction from baseline Kalman/MPC to Adaptive Kalman + AMPC and added direct workflow/task updates. | `Server/docs/technical/ADAPTIVE_KALMAN_AMPC_NOTES.md`, `Kalman/PRD.md`, `Kalman/README.md`, `Kalman/CLAUDE.md`, `.claude/CLAUDE.md`, `Server/docs/technical/ARCHITECTURE.md`, `Server/docs/technical/DECISIONS.md`, `Server/docs/technical/API.md`, `Server/docs/technical/DATABASE.md`, `Server/docs/technical/DESIGN_SYSTEM.md`, `Server/docs/user/USER_GUIDE.md`, `Server/docs/content/CONTENT_STRATEGY.md`, `Kalman/TODO.md`, `Kalman/.tasks/001-resolve-v1-estimation-decisions.md`, `Kalman/.tasks/002-design-experiment-data-model.md`, `Kalman/.tasks/004-arx-prediction-adapter.md`, `Kalman/.tasks/005-kalman-estimation-cycle.md`, `Kalman/.tasks/007-store-run-outputs-pipeline-logs.md`, `Kalman/.tasks/008-evaluation-metrics-report-export.md`, `Kalman/.tasks/009-raw-predicted-filtered-dashboard.md`, `Kalman/.tasks/012-v1-methodology-demo-workflow.md`, `Kalman/.tasks/013-synthesize-ampc-state-cost-safety-constraints.md` | Task mapping #000-#013 OK; active path scan found no bad root `.claude/.tasks` or non-`../ARX` data paths; new note and task #013 exist; no `Kalman/package.json`, so app tests were not run; no `.git` repo at workspace root. | Task #001 still must choose exact Adaptive Kalman rule, first state variable, prediction model path, and whether v1 includes an offline AMPC optimizer prototype. Source docs include rough research notes; use them as synthesis input, not unquestioned implementation truth. | Continue with #001 before coding estimator/storage so the implementation does not lock the wrong model boundary. |
| 2026-04-14 | Codex | Re-audited current direction after user asked whether `BaoCao.md`, `Tonghop.md`, `Tonghop2.md` were integrated. Confirmed ARX folder has CSVs, docs, notebooks, `arx_pipeline.py`, `arx_model.json`, generator/reporting tools; ran minimal ARX one-step smoke prediction from `arx_model.json` and `greenhouse_data.csv`; cleaned remaining stale PRD metric labels. | `Kalman/PRD.md`, `.claude/.claude/review/REVIEW.md` | ARX smoke check loaded 10 CSV rows and produced one-step prediction 57.2866 vs actual 57.6320 with residual 0.3454; task mapping #000-#013 OK; active path scan already OK; `Kalman/package.json` absent so app tests not run. | `Kalman/` still has no source code implementation yet, only docs/backlog/contracts; AKalman v1 should start after #001/#004 or with explicit default assumptions. ADR-001 and onboarding answers still contain historical baseline wording, superseded by ADR-002/current PRD. | Next implementation path: choose #001 defaults, then build ARX prediction adapter and Adaptive Kalman-ready estimator in #004/#005. |
| 2026-04-14 | Codex | Answered scope question: finishing the 13 open tasks #001-#013 means the Kalman v1 / AKalman prototype is complete, not the full final AMPC/closed-loop project. | `.claude/.claude/review/REVIEW.md` | Checked `Kalman/TODO.md` and `Kalman/PRD.md` scope from current context; no code changes. | Full AMPC closed-loop autonomous control remains out of v1 scope; AKalman code still must be implemented and verified before calling v1 done. | For the shortest runnable AKalman path, prioritize #001, #003, #004, #005, #008, then demo/log/view tasks. |
| 2026-04-14 | Codex | Added explicit skill metadata to task tracking so each Kalman task has one primary `required_skill` and optional `supporting_skills`; updated the task template for future tasks. | `Kalman/.tasks/TASK_TEMPLATE.md`, `Kalman/.tasks/000-initial-project-setup.md`, `Kalman/.tasks/001-resolve-v1-estimation-decisions.md`, `Kalman/.tasks/002-design-experiment-data-model.md`, `Kalman/.tasks/003-offline-dataset-ingestion-preprocessing.md`, `Kalman/.tasks/004-arx-prediction-adapter.md`, `Kalman/.tasks/005-kalman-estimation-cycle.md`, `Kalman/.tasks/006-experiment-configuration-persistence.md`, `Kalman/.tasks/007-store-run-outputs-pipeline-logs.md`, `Kalman/.tasks/008-evaluation-metrics-report-export.md`, `Kalman/.tasks/009-raw-predicted-filtered-dashboard.md`, `Kalman/.tasks/010-live-sensor-ingestion-path.md`, `Kalman/.tasks/011-missing-noisy-data-robustness-tests.md`, `Kalman/.tasks/012-v1-methodology-demo-workflow.md`, `Kalman/.tasks/013-synthesize-ampc-state-cost-safety-constraints.md`, `.claude/.claude/review/REVIEW.md` | Checked all 14 task files #000-#013 contain both `required_skill` and `supporting_skills`; template contains both fields. | Skill choices are project-level guidance; during implementation the active agent still needs to open/use the named skill before touching that task. | Start #001 with `software-architecture`, then #003/#004/#005 with `python-pro` and listed support skills as needed. |
| 2026-04-14 | Codex | Added rule for self-made agent-specific skill mirrors: Antigravity, Claude, Cursor, Codex, and OpenCode must use the matching hidden skill folder when borrowing a self-made mirrored skill. | `.claude/.claude/rules/agent-specific-skills.md`, `.claude/.claude/review/REVIEW.md` | Verified the five folders exist: `.antigravitySkill` 1401 SKILL.md files, `.claudeSkill` 1401, `.cursorSkill` 1404, `.codexSkill` 1406, `.opencodeSkill` 12; read back the new rule file. | Existing domain skill folders remain separate and should still be preferred when tasks name normal project/domain skills. | In Codex sessions, use `.claude/.claude/skills/.codexSkill/<skill-name>/SKILL.md` for self-made mirrored skills unless a better project/domain skill is explicitly named. |
| 2026-04-14 | Codex | Final pre-task readiness pass: fixed stale rule references to `tasks.md` and `tasks-manager/task/plan-*`, then validated task/skill mapping before implementation starts. | `.claude/.claude/rules/completion-checklist-sync.md`, `.claude/.claude/rules/be-fe-contract-sync.md`, `.claude/.claude/review/REVIEW.md` | `VALIDATION_OK`: TODO ids #000-#013 all map to 14 task files; all task files include required metadata; 19 referenced skills resolve in project domain root or `.codexSkill`; required ARX files exist; no stale `.claude/TODO`, `.claude/.tasks`, `.plans`, `.discuss`, `tasks.md`, or `tasks-manager` references found in active rules/CLAUDE files. | `Kalman/package.json` and `.git` are absent, so no app test/git status was run; #001 still intentionally contains open technical decisions before code should lock estimator behavior. | Proceed with #001 first, then #003/#004/#005 for the shortest runnable AKalman + ARX path. |
| 2026-04-14 | Codex | Initialized git repo at workspace root, added remote `https://github.com/BapTruongSinh/Kalman.git`, committed initial Kalman/ARX/workflow scaffold, and pushed `main`; intentionally did not create `package.json` because current runnable path is Python/ARX, not frontend/Vite yet. | `.gitignore`, `.claude/`, `ARX/`, `Kalman/`, `.claude/.claude/review/REVIEW.md` | Commit `0c99240` pushed to `origin/main`; `.gitignore` excludes the five large mirrored skill folders under `.claude/.claude/skills/.*Skill/`; staged commit contained no `package.json` and no hidden skill mirror paths. | A second review-memory commit is needed after this entry so the pushed repo records this action too; Python runtime/dependency packaging still needs to be decided in implementation task #003 or #004. | Commit and push this review-memory entry, then start #001. |
| 2026-04-14 | Codex | Answered ARX state question: current ARX code models `Soil_Moisture` as the single output/state-like variable, uses multiple exogenous inputs, and uses `theta` only as the parameter vector name, not as the greenhouse soil-water state. | `.claude/.claude/review/REVIEW.md` | Checked `ARX/arx_pipeline.py`, `ARX/arx_model.json`, `ARX/greenhouse_data.csv`, and ARX docs for `Soil_Moisture`, `Dr`, and `theta`. | `Dr` is not present as an ARX modeled output; choosing `Dr` for AMPC later would require derivation or an added model/state transformation. | For #001/#005, use `Soil_Moisture` / physical theta as the first estimator state if following ARX directly. |
| 2026-04-14 | Codex | Explained `Dr` vs `Soil_Moisture` conceptually for the greenhouse model: `Soil_Moisture` is measured soil water content/percentage; `Dr` is root-zone water depletion used in FAO-56/AMPC water-balance control. | `.claude/.claude/review/REVIEW.md` | No code change; answer derived from current ARX/AMPC project context. | Need soil parameters such as field capacity, wilting point, and root-zone depth before converting `Soil_Moisture` to `Dr` rigorously. | In #001, decide whether v1 estimates direct `Soil_Moisture` first and documents `Dr` as AMPC derived state. |
| 2026-04-14 | Codex | Answered ARX baseline/retrain/compare question: ARX code can retrain and perform order-selection internally, but Kalman v1 should treat saved ARX as fixed baseline adapter first; later comparison with LightGBM/XGBoost stays replaceable/future unless #001 explicitly pulls it into v1. | `.claude/.claude/review/REVIEW.md` | Checked `ARX/arx_pipeline.py` for `run_pipeline`, `estimate_ols`, `model_selection_search`, `save_artifact`; checked `Kalman/PRD.md`, task #001/#004, and ADR-002 for baseline/retraining scope. | PRD open question #2 still needs a formal decision; automatic retraining/model registry is explicitly out of v1. | In #001, set ARX mode to fixed baseline artifact for v1 unless the owner explicitly requests retrain/compare now. |
| 2026-04-14 | Codex | Answered CSV train/tune/eval split question: ARX artifact used 60/20/20 chronological split; Kalman v1 should avoid retraining and use CSV for offline replay/evaluation, preferably train slice for warmup/calibration, validation slice for parameter tuning, and test slice for final reported metrics. | `.claude/.claude/review/REVIEW.md` | Checked `ARX/arx_pipeline.py` `SplitConfig`, `split_time_series`, `model_selection_search`, `evaluate_slice`; checked `ARX/arx_model.json` split dates; checked PRD/task #001/#003/#008 CSV scope. | PRD open question #3 still needs formal decision; if v1 uses the full CSV for tuning and final evaluation, leakage risk must be documented. | In #001, lock CSV policy: ARX train from saved artifact, Kalman tuning on validation window, final evaluation on held-out test window. |
| 2026-04-14 | Codex | Superseded earlier fixed-baseline ARX guidance after the project owner chose retrain mode; ARX is now an explicit offline retrainable v1 baseline, not only a fixed saved artifact. | `Kalman/PRD.md`, `Kalman/.tasks/001-resolve-v1-estimation-decisions.md`, `Kalman/.tasks/004-arx-prediction-adapter.md`, `Server/docs/technical/DECISIONS.md`, `Server/docs/technical/ARCHITECTURE.md`, `.claude/.claude/review/REVIEW.md` | Verified updated docs/tasks mention retrainable/offline ARX, chronological train/validation/test split, and exclusion of automatic online retraining/model registry from v1. | Task #001 still must decide adaptive rule, first state/covariance/noise defaults, storage format, offline/live priority, and AMPC optimizer scope. | Implement #004 adapter with explicit offline retrain, artifact load, and predict phases. |
| 2026-04-14 | Codex | User hỏi lại việc tuân thủ rules; đã đọc lại toàn bộ `REVIEW.md` và toàn bộ 17 file trong `.claude/.claude/rules/`, xác nhận rule preflight và rule review-memory là bắt buộc trước/sau mỗi prompt làm việc. | `.claude/.claude/review/REVIEW.md`, `.claude/.claude/rules/*.md` | Đã liệt kê đủ 17 rule files trong `rules/` và đọc lại `REVIEW.md`; không thay đổi code dự án ngoài entry review này. | Các prompt trước trong phiên này chưa đi đúng đủ preflight theo rule mới nhắc lại; từ đây cần giữ đúng quy trình read-rules/read-review trước việc mới và append review sau mỗi cửa sổ làm việc. | Ở prompt tiếp theo, bắt đầu bằng preflight đúng chuẩn rồi mới tiếp tục task người dùng giao. |
| 2026-04-14 | Codex | Audit lại toàn bộ workflow trong `.claude` và bộ tài liệu/task trong `Kalman` để xác định quy trình tạo prompt/task và mức độ sẵn sàng kiến thức trước khi bắt đầu implementation. | `.claude/.claude/review/REVIEW.md`, `.claude/.claude/rules/*.md`, `.claude/**/*`, `Kalman/**/*` | Đã đọc lại `REVIEW.md`, 17 rule files; quét toàn bộ cây `.claude` và `Kalman`; dùng audit read-only để tổng hợp workflow `.claude` và readiness của `Kalman`. Kết luận: hiểu rõ operating process; `Kalman` hiện chủ yếu là docs/backlog, chưa có source code app; đủ kiến thức để bắt đầu task #001, còn task implementation #002-#005 cần #001 chốt open decisions trước. | Một số lệnh/hook trong `.claude` còn tham chiếu path cũ root-level thay vì `Kalman/`; metadata dependency giữa #013 và #012 còn lệch nhẹ; các quyết định về state, adaptive rule, ARX usage, storage vẫn mở trong PRD. | Khi bắt đầu làm thật, đi theo thứ tự #001 trước, rồi song song #002/#003/#013 sau khi chốt quyết định v1. |
| 2026-04-14 | Codex | Chuẩn hóa các câu trả lời mới của project owner cho task #001: state đầu tiên, ARX retrain policy, data-source abstraction từ MySQL/CSV snapshot, adaptive `R` rule, bộ default Kalman config, storage local MySQL, và scope AMPC v1 chỉ ở mức docs/contracts. | `.claude/.claude/review/REVIEW.md`, `Kalman/.tasks/001-resolve-v1-estimation-decisions.md`, `Kalman/PRD.md`, `Server/docs/technical/DECISIONS.md`, `Server/docs/technical/ARCHITECTURE.md`, `ARX/arx_pipeline.py` | Đọc lại task #001, PRD open questions, ADRs, architecture, và ARX output/input definitions; không chỉnh docs ở bước này, chỉ tổng hợp quyết định để báo cáo lại user. | Vẫn còn ít nhất 3 điểm chưa được user chốt trực tiếp trong prompt này: offline-first vs live-from-start, visualization minimum (plots hay full dashboard), và ngưỡng “good enough” chính thức cho Kalman ngoài variance reduction. | Nếu user duyệt bộ quyết định đã chuẩn hóa, bước tiếp theo là ghi chúng vào `PRD.md`, `DECISIONS.md`, `ARCHITECTURE.md` và task #001 để mở đường cho #002/#003/#004/#005. |
| 2026-04-14 | Codex | Fix 3 review findings (round 2) cho task #002: (1-Med) thêm `id = models.BigAutoField(primary_key=True)` rõ ràng vào `PipelineCycle` để model khớp migration + DEFAULT_AUTO_FIELD; (2-Low/Med) sửa `evaluation_summaries.slice_type` trong DATABASE.md từ ENUM → VARCHAR(15) (cái cuối còn sót); (3-Low) sửa `sample_ts` từ `DATETIME(3)` → `DATETIME` trong DATABASE.md vì Django `DateTimeField()` không encode sub-second precision. | `Server/backend/estimation/models.py`, `Server/docs/technical/DATABASE.md`, `.claude/.claude/review/REVIEW.md` | ReadLints 0 errors sau fix; py_compile pass trước đó. | Sau khi cài deps: chạy `python manage.py check` và `python manage.py makemigrations --check --dry-run` để xác nhận migration không lệch model; nếu cần millisecond precision cho `sample_ts` thì cần custom DDL hoặc migration `RunSQL`. | Task #002 đã sạch. |
| 2026-04-14 | Codex | Hoàn thành task #003 — offline dataset ingestion & preprocessing: tạo package `estimation/ingestion/` gồm `loader.py` (RawRecord frozen dataclass, load_csv với header/timestamp guard, split_chronological 60/20/20), `validator.py` (ValidationResult, ValidationConfig với physical bounds, validate_record với missing/out_of_range/nan/suspicious_repeat, validate_batch với rolling context), `preprocessor.py` (ProcessedRecord, 3 policies: keep_last/interpolate/skip), `__init__.py` public API; viết 30 pytest cases trong `estimation/tests/test_ingestion.py` bao gồm happy path, edge case, và end-to-end smoke test trên greenhouse_data.csv (105 120 rows) — **30/30 passed**. Cập nhật task #003 completed, TODO.md, REVIEW.md. | `Server/backend/estimation/ingestion/__init__.py`, `Server/backend/estimation/ingestion/loader.py`, `Server/backend/estimation/ingestion/validator.py`, `Server/backend/estimation/ingestion/preprocessor.py`, `Server/backend/estimation/tests/__init__.py`, `Server/backend/estimation/tests/test_ingestion.py`, `Kalman/.tasks/003-offline-dataset-ingestion-preprocessing.md`, `Kalman/TODO.md`, `.claude/.claude/review/REVIEW.md` | pytest 30/30 passed; ReadLints 0 errors trên 4 file mới; py_compile pass; smoke test load → split → validate → preprocess toàn bộ train slice (63 072 records) không có None soil_moisture. | `apply_preprocessing(interpolate)` dùng `_last_valid_index` tìm lùi từ `processed_so_far` — hoạt động đúng nhưng O(n²) trong trường hợp xấu; đủ cho v1 với 105k rows. Chưa có MySQL ingestion path (task #010). | Task tiếp theo: #004 — ARX prediction adapter (blocked_by: #001 ✅, #003 ✅). |
| 2026-04-14 | Codex | Fix 7 review findings cho task #003 sau audit: **(High)** `preprocessor.py` — `keep_last` và `interpolate` chỉ thay thế field `None`, bỏ sót out_of_range non-None (e.g. `sm=101`) → sửa: khi `not vr.is_valid`, thay thế ALL fields bằng last valid / interpolated, bỏ `if raw_val is not None: keep` shortcut; `skip` cũng đổi thành set ALL fields → `None` khi invalid; rewrite `_apply_interpolate` dùng `last_valid_idx` dict thay `_last_valid_index` O(n²); xoá helper `_last_valid_index` không còn dùng. **(Med)** `validator.py:156` — repeat detection filter `if r.soil_moisture is not None` nhưng vẫn check `len(window)` → false positive với `[None, 55]` + threshold=3; sửa: dùng `comparable = [r for r in window if r.sm is not None]` và check `len(comparable) >= threshold-1`. **(Med)** `test_ingestion.py` — 4 dataset tests dùng `pytest.skip` → đổi thành `pytest.fail` vì CSV là required fixture trong repo. **(Low)** `loader.py:split_chronological` — không sort → sửa: thêm `records = sorted(records, key=lambda r: r.timestamp)` trước split; thêm minimum-size guard `if n_train<1 or n_val<1 or n_test<1: raise ValueError(...)`. **(Low)** tạo `requirements-dev.txt` với `pytest>=7.0`. **(Low)** tests — tighten interpolation test `50<=v<=60` → `pytest.approx(55.0)`; thêm 6 test mới: `test_out_of_range_replaced_not_passed_through`, `test_out_of_range_set_to_none_when_skipped`, `test_out_of_range_interpolated_not_forwarded`, `test_repeat_no_false_positive_with_none_in_window`, `test_raises_on_too_small_dataset`, `test_sort_corrects_out_of_order_input`. | `Server/backend/estimation/ingestion/preprocessor.py`, `Server/backend/estimation/ingestion/validator.py`, `Server/backend/estimation/ingestion/loader.py`, `Server/backend/estimation/tests/test_ingestion.py`, `Server/backend/requirements-dev.txt`, `.claude/.claude/review/REVIEW.md` | pytest **36/36 passed** (6.26s); py_compile 0 errors; `manage.py check` 0 issues; `makemigrations --check --dry-run` → "No changes detected"; ReadLints 0 errors trên 4 files. | Không còn O(n²) trong interpolate sau fix; `_dummy_records` giờ dùng unique timestamp sequence (timedelta) thay vì same-timestamp để `test_chronological_order_preserved` có ý nghĩa thật. | Task tiếp theo: #004 — ARX prediction adapter (blocked_by: #001 ✅, #003 ✅). |
| 2026-04-15 | Agent | Task #008 done — thêm `estimation/evaluation/` package: `SliceMetrics` dataclass (26 metrics + ADR-003 flags), `compute_metrics()` pure (no Django), `evaluate_slice/all_slices` DB, `build_text_report`, `export_to_csv`, `export_plots` (optional matplotlib); migration 0003 (`latency_ms` → PipelineCycle, `n_r_updated/skipped/adaptive_skipped` + `latency_mean/p95_ms` → EvaluationSummary); 53 new tests; `__init__.py` lazy-import pattern; ARCHITECTURE.md Evaluation section added; requirements.txt updated. Full suite: 390 passed. | `estimation/evaluation/__init__.py`, `evaluation/metrics.py`, `evaluation/reporter.py`, `estimation/migrations/0003_evaluation_latency_adaptive_counts.py`, `estimation/tests/test_evaluation.py`, `estimation/models.py`, `estimation/pipeline/store.py`, `docs/technical/ARCHITECTURE.md`, `requirements.txt`, `Kalman/.tasks/008-evaluation-metrics-report-export.md`, `Kalman/TODO.md` | 390 passed; manage.py check clean; makemigrations --check → No changes detected. | matplotlib/seaborn 3.x ABI mismatch vs numpy 2.x still possible; export_plots() returns [] with RuntimeWarning instead of crashing. | Tiếp tục task #009 (dashboard) hoặc #011 (robustness tests). |
| 2026-04-15 | Cursor | Task #012 hoàn tất — tài liệu methodology + demo: thêm `docs/technical/METHODOLOGY_V1.md` (ARX/Kalman/innovation/adaptive R/evaluation/AMPC contracts + out-of-scope rõ ràng); viết lại `docs/user/USER_GUIDE.md` (prereq, dashboard+Vite proxy, API, recipe Django shell CSV→DB với `evaluate_all_slices`, pytest reproduce); liên kết từ `ARCHITECTURE.md`, `API.md`, `CLAUDE.md`; cập nhật task #012 + `TODO.md` (#013 lên Up Next). | `Server/docs/technical/METHODOLOGY_V1.md`, `Server/docs/user/USER_GUIDE.md`, `Server/docs/technical/ARCHITECTURE.md`, `Server/docs/technical/API.md`, `Kalman/CLAUDE.md`, `Kalman/.tasks/012-v1-methodology-demo-workflow.md`, `Kalman/TODO.md`, `.claude/.claude/review/REVIEW.md` | Đối chiếu code `run_config`, `pipeline/store`, `evaluation` exports và dashboard `vite.config.ts` proxy; chưa chạy `manage.py shell` full 105k rows trong phiên. | Recipe shell giả định `cwd` là `Server/backend`; full CSV nặng DB — doc đã ghi slice tùy chọn. | Task #013 (AMPC synthesis) hoặc chạy thử shell replay subset. |
| 2026-04-15 | Cursor | Task #011 hoàn tất — robustness tests cho pipeline v1: `estimation/tests/test_pipeline_robustness.py` (8 tests) bao missing/malformed/OOR/suspicious-repeat/noise, loader skip timestamp, validate_batch có `reason`, full chain preprocess + `AdaptiveKalmanCycle.replay` không crash, subset `greenhouse_data.csv` inject lỗi, split→train slice; dọn import thừa, bỏ `@pytest.mark.django_db` không cần; cập nhật `ARCHITECTURE.md` mục Testing and data robustness; task #011 completed + `Kalman/TODO.md`. | `Server/backend/estimation/tests/test_pipeline_robustness.py`, `Server/docs/technical/ARCHITECTURE.md`, `Kalman/.tasks/011-missing-noisy-data-robustness-tests.md`, `Kalman/TODO.md`, `.claude/.claude/review/REVIEW.md` | `python -m pytest estimation/tests/test_pipeline_robustness.py -v` → **8/8 passed** (~1.8s); ReadLints file test: 0. | Chưa chạy full `pytest estimation/tests` trong phiên này sau chỉnh import; nên chạy full suite trước merge nếu cần. | Task #012 giờ đủ dependency #011; có thể làm #012 (methodology + demo workflow). |
| 2026-04-14 | Codex | Fix migration drift (round 3) cho task #002: (1) 4 `AutoField` tự sinh trong 0001_initial.py thiếu `auto_created=True, verbose_name="ID"` — sửa tại lines 31 (ExperimentRun), 89 (ExperimentConfig), 153 (ARXArtifact), 355 (EvaluationSummary); (2) `input_cols_json.help_text` trong migration lệch model — thêm `(e.g. Temperature, Humidity, ...)` cho khớp `models.py:145`. `PipelineCycle.id` ở line 197 giữ nguyên `BigAutoField` không thêm `auto_created` vì đó là field khai báo tường minh, không phải field tự sinh. | `Server/backend/estimation/migrations/0001_initial.py`, `.claude/.claude/review/REVIEW.md` | Grep xác nhận 4 dòng AutoField và 1 dòng help_text đúng sau fix; ReadLints 0 errors. | Verification tiếp theo: `python manage.py makemigrations --check --dry-run` — nếu không sinh migration mới là sạch hoàn toàn; sau đó `python manage.py migrate` để tạo bảng thật. | Task #002 đã sạch sau 3 vòng review. |
| 2026-04-14 | Codex | Fix 5 review findings cho task #002: (1-High) viết thủ công `0001_initial.py` cho toàn bộ 5 bảng vì Django chưa install trong env; (2-Med) đổi tất cả ENUM → VARCHAR+Django choices trong DATABASE.md; (3-Med) thêm `UniqueConstraint(run, cycle_index)` vào `PipelineCycle.Meta` và xoá `idx_cycles_run_index` (redundant); (4-Med) thêm `load_dotenv()` vào settings.py với guard `try/except` để không crash nếu package chưa install; (5-Low) sửa 2 query `SELECT *` → explicit field list trong DATABASE.md. | `Server/backend/estimation/migrations/0001_initial.py`, `Server/backend/estimation/models.py`, `Server/backend/config/settings.py`, `Server/docs/technical/DATABASE.md`, `.claude/.claude/review/REVIEW.md` | ReadLints sau fix: 0 linter errors trên tất cả file đã sửa; kiểm tra tree backend đủ 14 file. | Migration `0001_initial.py` là handwritten — cần verify lại sau khi `pip install -r requirements.txt` và chạy `python manage.py migrate` thật; nếu Django detect diff giữa migration và models.py, cần chạy `makemigrations --check` để catch lệch. | Task #002 đã clean; có thể tiếp tục với #003/#004. |
| 2026-04-14 | Codex | Thiết kế schema đầy đủ cho task #002 (5 bảng: experiment_runs, experiment_configs, arx_artifacts, pipeline_cycles, evaluation_summaries); viết lại toàn bộ `docs/technical/DATABASE.md` với tất cả cột, index, query patterns, và quyết định storage (MySQL là source of truth, CSV là snapshot/export); tạo Django ORM models (`backend/estimation/models.py`), backend scaffold (`manage.py`, `config/settings.py`, `requirements.txt`, `.env.example`); chốt storage format decision từ ADR-003; cập nhật task #002 sang completed, dịch chuyển trong TODO.md. | `Server/docs/technical/DATABASE.md`, `Server/backend/estimation/models.py`, `Server/backend/config/settings.py`, `Server/backend/manage.py`, `Server/backend/requirements.txt`, `Server/backend/.env.example`, `Kalman/.tasks/002-design-experiment-data-model.md`, `Kalman/TODO.md`, `.claude/.claude/review/REVIEW.md` | Đọc lại REVIEW.md (preflight), task #002, DATABASE.md hiện tại, CSV columns, PRD FR-013/014/015/016/NFR-006/007; không có linter error trên models.py và settings.py. | Django project chưa có `urls.py`, `wsgi.py`, admin, REST API — những thứ này sẽ đến trong task #003/#006 khi dựng pipeline thật; migration `0001_initial.py` chưa được generate (cần `python manage.py makemigrations` sau khi cài đủ deps); AMPC-derived columns (`Dr`, `ETc`) intentionally deferred sang task #013. | Task tiếp theo: #003 (offline ingestion + preprocessing) hoặc #004 (ARX adapter) — cả hai đã được unblock bởi #001/#002. |
| 2026-04-14 | Codex | Ghi chính thức các quyết định đã chốt của task #001 vào docs và task tracking: state đầu tiên là `Soil_Moisture`, ARX là baseline retrainable offline, replay offline-first với split thời gian `60/20/20`, adaptive Kalman v1 chỉ adaptive `R`, MySQL là source of truth cục bộ, AMPC v1 chỉ ở mức docs/contracts, và bộ tiêu chí held-out test đã được khóa; đồng thời chuyển `#001` sang completed trong `Kalman/TODO.md`. | `Kalman/PRD.md`, `Server/docs/technical/DECISIONS.md`, `Server/docs/technical/ARCHITECTURE.md`, `Kalman/.tasks/001-resolve-v1-estimation-decisions.md`, `Kalman/TODO.md`, `.claude/.claude/review/REVIEW.md` | Đã rà lại 5 file đã sửa; `ReadLints` không báo lỗi; nội dung task/docs/TODO đồng bộ với nhau. | AWS hosting vẫn deferred; task #002 cần chốt schema/bảng cụ thể; task #013 vẫn cần tổng hợp state/cost/constraint chi tiết hơn ở mức controller. | Bắt đầu implementation path với #002 hoặc #003/#004 tùy muốn chốt data model trước hay dựng replay pipeline trước. |
| 2026-04-15 | Cursor | Task #013 hoàn tất — tổng hợp AMPC implementation-ready: thêm `Server/docs/technical/AMPC_MODELING_HANDOFF.md` (θ vs Dr, u vector khớp CSV Drip/Mist/Fan, nhiễu đo được + ET0/ETc planned, J cost zone/water/energy/switching/Δu, bảng safety+fallback nối `PipelineCycle`/Kalman, FR-025–027 traceability, khẳng định v1 **không** có QP/NLP offline AMPC prototype trong code); liên kết `ARCHITECTURE.md`, `METHODOLOGY_V1.md`, `DATABASE.md` (open item), `USER_GUIDE.md`, `CLAUDE.md`; đóng task #013 + `Kalman/TODO.md`. | `Server/docs/technical/AMPC_MODELING_HANDOFF.md`, `Server/docs/technical/ARCHITECTURE.md`, `Server/docs/technical/METHODOLOGY_V1.md`, `Server/docs/technical/DATABASE.md`, `Server/docs/user/USER_GUIDE.md`, `Kalman/CLAUDE.md`, `Kalman/.tasks/013-synthesize-ampc-state-cost-safety-constraints.md`, `Kalman/TODO.md`, `.claude/.claude/review/REVIEW.md` | `python manage.py check` OK; `python -m pytest estimation/tests/test_pipeline_robustness.py -q` → 8 passed. | ET/Dr cần nguồn ngoài CSV và hiệu chuẩn thủy lực trước khi persist cột DB; controller v2 vẫn ngoài scope. | Bảo trì docs khi đổi interface estimator; bắt đầu v2 chỉ sau khi có mô hình nhà máy/ET. |
| 2026-05-08 | Codex | Wrote cleanup plan for switching Kalman runtime to online/live ingestion with pretrained `../ARX/arx_model.json`, removing offline replay and in-app ARX training paths. Used `concise-planning`; read `CLAUDE.md`, project rules, review memory, `CODEBASE_ONBOARDING.md`, TODO, ADRs, architecture, and relevant backend modules before writing. | `Server/docs/plan/2026-05-08-online-arx-model-cleanup.md`, `.claude/.claude/review/REVIEW.md` | Verified plan file content with `Get-Content`; `git status --short` shows this plan plus pre-existing modified/untracked files. No app tests run because this was planning-only. | `Kalman/.understand-anything/knowledge-graph.json` does not exist yet, so implementation should start by running `understand` or documenting that plugin dependency is unavailable. Existing ADR-003 says offline/retrainable baseline; implementation should add a superseding ADR if user approves the new online/artifact-only direction. | Wait for user review/approval of the plan, then implement top-down: understand graph, dependency audit, artifact-only ARX adapter, live ingest ARX prior, remove replay/retrain leftovers, update tests/docs. |
| 2026-05-08 | Codex | Implemented online ARX artifact cleanup: live ingest loads cached `ARXPredictionAdapter` from `settings.ARX_MODEL_PATH`, reconstructs per-run live history from prior `PipelineCycle` rows, and passes adapter into `AdaptiveKalmanCycle`; runtime prediction contract no longer forces `train()`/`save_artifact()`. Added SQLite DB override for validation, ARX model path setting, live ingest ARX-prior test, ADR-004, and synced architecture/API/database/user/methodology docs plus plan checkboxes. | `Server/backend/config/settings.py`, `Server/backend/estimation/api/ingest.py`, `Server/backend/estimation/prediction/base.py`, `Server/backend/estimation/prediction/__init__.py`, `Server/backend/estimation/tests/test_live_ingest.py`, `Server/backend/.env.example`, `Server/docs/technical/DECISIONS.md`, `Server/docs/technical/ARCHITECTURE.md`, `Server/docs/technical/API.md`, `Server/docs/technical/DATABASE.md`, `Server/docs/technical/METHODOLOGY_V1.md`, `Server/docs/user/USER_GUIDE.md`, `Server/docs/plan/2026-05-08-online-arx-model-cleanup.md`, `.claude/.claude/review/REVIEW.md` | `py_compile` pass; default `python manage.py check` pass; SQLite override `manage.py check` pass; `makemigrations --check --dry-run` no changes; targeted tests pass (`test_prediction.py` 66, `test_kalman.py` 84, `test_live_ingest.py` 48, `test_pipeline_store.py` 60, `test_api.py` 28); full `python -m pytest estimation/tests -q` with SQLite override: 503 passed. | Local default MySQL/XAMPP is not running, so DB-backed pytest under default MySQL fails at connection setup; SQLite override was used for repeatable validation. `understand`/`understand-explain` could not run because `pnpm` and built understand core were missing. Offline replay/train helpers remain as documented legacy research/test surface. `CODEBASE_ONBOARDING.md` was intentionally not updated before user review approval. | If owner wants hard deletion of offline replay/train support, schedule a separate migration/test rewrite after deciding what to do with historical rows and evaluation reports. |
| 2026-05-08 | Codex | Follow-up cleanup per owner: removed SQLite DB override, removed offline/replay/train app surface, converted schema to live-only (`run_type=live`, `source_type=live`, `slice_type=online`), dropped `ARXArtifact`, removed split/train config fields, removed `AdaptiveKalmanCycle.replay()`, removed app-side `ARXPredictionAdapter.train()` / native `save_artifact()`, replaced tests with live-only coverage, and updated active docs/plan. Re-read `understand`; built plugin core with pnpm fallback, but full graph phases still require unavailable subagent dispatcher in Codex. | `Server/backend/config/settings.py`, `Server/backend/.env.example`, `Server/backend/estimation/models.py`, `Server/backend/estimation/migrations/0006_live_only_cleanup.py`, `Server/backend/estimation/ingestion/`, `Server/backend/estimation/prediction/`, `Server/backend/estimation/kalman/cycle.py`, `Server/backend/estimation/pipeline/store.py`, `Server/backend/estimation/evaluation/`, `Server/backend/estimation/api/`, `Server/backend/estimation/tests/`, `Server/docs/technical/{API,ARCHITECTURE,DATABASE,DECISIONS,METHODOLOGY_V1}.md`, `Server/docs/user/USER_GUIDE.md`, `Server/docs/plan/2026-05-08-online-arx-model-cleanup.md`, `.claude/.claude/review/REVIEW.md` | MySQL: `python manage.py migrate` OK; `python -m pytest estimation/tests -q` -> 80 passed; `python manage.py check` OK; `python manage.py makemigrations --check --dry-run` -> No changes detected; `python manage.py migrate --check` OK; backend grep shows no SQLite override / offline enum / split / replay / app train/save surface except historical migrations and external `arx_pipeline.save_artifact` docstring references. | `CODEBASE_ONBOARDING.md` intentionally not updated before user review approval; older historical docs under `docs/Why` / review-history still mention removed concepts. Migration 0006 deletes derived `EvaluationSummary` rows to avoid train/validation/test unique collisions; recompute online summaries if needed. | If user approves, update `CODEBASE_ONBOARDING.md` to reflect live-only architecture and optionally prune obsolete historical docs. |
| 2026-05-08 | Codex | Fixed review findings on live-only cleanup: `0006` now backfills live dedupe keys for fresh DBs; new `0007` repairs already-migrated MySQL rows, deletes duplicate `(run_id, sample_ts)` rows before key backfill, removes `preprocessing_policy`, normalizes `preprocess_status` to live-only `valid/skipped`, and adds DB check `run_type='live'`. Service `create_run()` no longer accepts caller-provided run type; ingestion preprocessing no longer exports batch keep-last/interpolate policy surface. | `Server/backend/estimation/migrations/0006_live_only_cleanup.py`, `Server/backend/estimation/migrations/0007_live_dedupe_key_and_config_cleanup.py`, `Server/backend/estimation/models.py`, `Server/backend/estimation/run_config/{config.py,service.py}`, `Server/backend/estimation/ingestion/{__init__.py,preprocessor.py}`, `Server/backend/estimation/kalman/cycle.py`, `Server/backend/estimation/tests/{test_ingestion.py,test_run_config.py}`, `Server/docs/technical/{API,ARCHITECTURE,DATABASE}.md`, `Server/docs/plan/2026-05-08-online-arx-model-cleanup.md`, `.claude/.claude/review/REVIEW.md` | MySQL `python manage.py migrate` applied 0007 OK; DB verification: `prefix_counts=(('live', 105120),)`, duplicate `(run_id, sample_ts)` groups `0`, run types `live` only, ORM bypass with `run_type='offline_replay'` raises `IntegrityError`; `python -m pytest estimation\tests -q` -> 80 passed; `python manage.py check` OK; `makemigrations --check --dry-run` no changes; `migrate --check` OK; `git diff --check` only CRLF warnings. | Historical migrations still contain old enum/policy values by design; `RunConfig.from_json()` still drops old `preprocessing_policy` key for backward-compatible raw JSON loading. `CODEBASE_ONBOARDING.md` still not updated until user review approval. | If accepted, update `CODEBASE_ONBOARDING.md`; otherwise next review should focus on historical docs under `docs/Why` / `KALMAN_FOLDER_REVIEW.md` if owner wants them pruned too. |
| 2026-05-08 | Codex | Tightened duplicate timestamp handling in live-only migrations after review: `0006`/`0007` no longer blindly delete all duplicate `(run_id, sample_ts)` rows. They now compare raw sensor payloads and only auto-delete identical-payload duplicates when the duplicate group is at the run tail; conflicting payloads or non-tail duplicates raise `ValueError` with run/sample/IDs/cycle-index details for manual repair. | `Server/backend/estimation/migrations/0006_live_only_cleanup.py`, `Server/backend/estimation/migrations/0007_live_dedupe_key_and_config_cleanup.py`, `.claude/.claude/review/REVIEW.md` | `py_compile` OK for both migrations; smoke script with transaction rollback passed: `mid_run_raise_ok`, `payload_conflict_raise_ok`, `tail_identical_delete_ok`; `python manage.py check` OK; `makemigrations --check --dry-run` no changes; `migrate --check` OK; `python -m pytest estimation\tests -q` -> 80 passed; `git diff --check` only CRLF warnings. | Because local MySQL already had 0007 applied and no duplicates, this is a migration-file safety correction for future/fresh environments; no schema state change needed. | Keep this fail-fast pattern for future stateful Kalman data migrations. |
| 2026-05-08 | Codex | Removed remaining low-priority live-only cleanup leftovers: dropped app CSV loader and batch/full-record validator surface, kept only `RawRecord`, `validate_live_record`, and `preprocess_single`; removed dead `skipped_invalid` runtime enum/status via migration `0008`; cleaned active docs/model help text that still mentioned test/validation/offline wording. | `Server/backend/estimation/ingestion/{__init__.py,loader.py,validator.py}`, `Server/backend/estimation/models.py`, `Server/backend/estimation/evaluation/metrics.py`, `Server/backend/estimation/run_config/config.py`, `Server/backend/estimation/migrations/0008_live_status_and_text_cleanup.py`, `Server/backend/estimation/tests/test_ingestion.py`, `Server/docs/technical/{API,ARCHITECTURE,DATABASE,METHODOLOGY_V1}.md`, `.claude/.claude/review/REVIEW.md` | MySQL `python manage.py migrate` applied 0008 OK; DB check showed `cycle_status_counts=(('ok', 105120),)` and `skipped_invalid_count=0`; `python -m pytest estimation\tests -q` -> 78 passed; `python manage.py check` OK; `makemigrations --check --dry-run` no changes; `migrate --check` OK; runtime grep outside migrations found no `load_csv`/`validate_batch`/`validate_record`/`skipped_invalid`; active docs grep clean; `git diff --check` only CRLF warnings. | Historical migrations still contain old CSV/offline/status strings by design. Test count decreased from 80 to 78 because CSV loader tests were removed with the dead loader. | If owner approves, update `CODEBASE_ONBOARDING.md` to reflect final live-only ingestion surface. |
| 2026-05-08 | Codex | Fixed dashboard/backend contract drift: `RunDashboard` no longer defaults to or exposes `train`/`validation`/`test`; it requests `slice=online` and renders a single online chart. Metrics panel canonical order is now `online`; frontend fixtures/tests use `run_type='live'` and `slice_type='online'` / `slice=online`. | `Server/dashboard/src/components/{RunDashboard.tsx,MetricsPanel.tsx,SliceChart.tsx}`, `Server/dashboard/src/__tests__/{apiClient.test.ts,MetricsPanel.test.tsx,RunSelector.test.tsx,SliceChart.test.tsx,AdaptiveStatusBar.test.tsx}`, `.claude/.claude/review/REVIEW.md` | Dashboard: `npm test -- --run` -> 5 files / 30 tests passed; `npm run build` OK with only Vite chunk-size warning. Backend regression: `python -m pytest estimation\tests -q` -> 78 passed; `python manage.py check` OK; `makemigrations --check --dry-run` no changes; `migrate --check` OK. FE grep found no legacy offline/slice defaults. `git diff --check` only CRLF warnings. | Build warning remains: generated JS chunk >500 kB; unrelated to live-only contract and existed as bundle-size guidance. | If user wants, add a dashboard regression test around `RunDashboard` fetch params specifically; current api client test covers `slice=online`, build verifies TS. |
| 2026-05-08 | Codex | Updated `CODEBASE_ONBOARDING.md` after user approval to reflect the accepted live-only architecture: MySQL-only backend, ARX artifact runtime, live ingestion/preprocessing, live-only schema constraints, dashboard online contract, validation gates, and AMPC/MPC boundary. | `Server/docs/technical/CODEBASE_ONBOARDING.md`, `.claude/.claude/review/REVIEW.md` | Read back first section and ran grep for obsolete names; matches are only intentional "no longer" notes / grep examples. `git diff --check` has only CRLF warnings. No app tests rerun because this was documentation-only after already-passing backend/frontend gates. | File was rewritten cleanly, so older explanatory detail about deleted offline paths is intentionally gone. | For MPC implementation planning, use `AMPC_MODELING_HANDOFF.md` plus this onboarding as the starting context. |
| 2026-05-08 | Codex | Ran MPC onboarding equivalent from `.claude/START_HERE.md` for new target `MPC/`: created root-level controller project scaffold, saved all MPC Q&A, wrote PRD/README/CLAUDE/TODO/docs/plan, created real `.tasks` backlog #000-#008, and updated global rules so future work can target either `Kalman/` or `MPC/`. No MPC runtime code was implemented yet. | `MPC/`, `.claude/CLAUDE.md`, `.claude/START_HERE.md`, `.claude/.claude/rules/{preflight-check.md,completion-checklist-sync.md,task-skill-gates.md}`, `.claude/.claude/review/REVIEW.md` | Required file check passed; TODO IDs #000-#008 all have matching task files; placeholder grep found only intentional wording in `MPC/PRD.md`. | `MPC/` is docs/backlog scaffold only; task #001 must still design package/config before writing controller code. Some open implementation defaults remain: grid resolution, cost weights, soft daily cap, real actuator endpoint. | Start `MPC` task #001 before implementing solver/runtime. |
| 2026-05-08 | Codex | Fixed MPC onboarding review findings: generalized `.claude/START_HERE.md` to use `{TARGET}` instead of hard-coded Kalman paths, removed root `.claude/CLAUDE.md` placeholders and made critical rules target-aware, added `required_skills` and `## Completion Gates` to MPC task template plus tasks #000-#008, expanded `MPC/CLAUDE.md` workflow with preflight/skills/4-gate/onboarding rules, and updated task-skill gate mapping for local MPC skills. | `.claude/START_HERE.md`, `.claude/CLAUDE.md`, `.claude/.claude/rules/task-skill-gates.md`, `MPC/CLAUDE.md`, `MPC/.tasks/*.md`, `.claude/.claude/review/REVIEW.md` | Checks passed: root CLAUDE placeholder grep clean; START_HERE hard-coded target path grep clean; MPC tasks all have `required_skills` and 4 gates (`required_skills`, `Completion Gates`, `Logic`, `Nghiệp vụ`, `Security`, `Test chạy thực tế` count = 10 incl. template); TODO/task mapping #000-#008 OK; `git diff --check` only LF/CRLF warnings. | `MPC/` still has no runtime package; task #001 remains next. `START_HERE` still contains generic onboarding prompt examples by design, but no longer hard-codes Kalman output paths in copy/update/task steps. | Begin `MPC` #001 only after reading the updated rules/task file and using required skills. |
| 2026-05-08 | Codex | Completed MPC task #001: designed package/module boundaries, public dataclass contracts, CLI layout, default config schema, grid resolution, default cost weights, soft daily cap, and ADR-004 for v2 MPC/v3 AMPC implementation. | `MPC/TODO.md`, `MPC/.tasks/001-design-mpc-package-architecture.md`, `MPC/docs/technical/ARCHITECTURE.md`, `MPC/docs/technical/API.md`, `MPC/docs/technical/CONFIG.md`, `MPC/docs/technical/DECISIONS.md`, `.claude/.claude/review/REVIEW.md` | Structural checks passed: config/default grep found required values; TODO IDs #000-#008 all map to `.tasks`; touched files have no trailing whitespace; secret URL/token grep found no hardcoded URL/token; task #001 checkboxes and TODO Completed are synced. No runtime tests because `MPC/` has no package code yet and task #001 is planning/docs only. | Defaults are demo/research starting values, not agronomy field calibration; `MPC/docs/technical/CODEBASE_ONBOARDING.md` intentionally not updated until user review approval. `MPC/` remains untracked in git status from prior scaffold. | Start task #002 to implement ARX plant adapter and state history contracts against the frozen config/interface. |
| 2026-05-08 | Codex | Fixed review findings after MPC task #001: synced PRD/Q&A open-item wording with ADR-004 defaults, corrected docs that implied package runtime was created by task #001, and added `CONFIG.md` to MPC/root read-order docs. | `MPC/PRD.md`, `MPC/docs/technical/ONBOARDING_ANSWERS.md`, `MPC/README.md`, `MPC/CLAUDE.md`, `MPC/docs/technical/CODEBASE_ONBOARDING.md`, `.claude/CLAUDE.md`, `.claude/.claude/review/REVIEW.md` | Grep for stale phrases (`Default sẽ chọn ở task #001`, `Chưa chốt`, `task #001 tạo package`, `Code package sẽ được tạo trong task #001`) returned no matches; grep confirmed ADR-004 defaults and `CONFIG.md` references in PRD/Q&A/README/CLAUDE/onboarding; trailing-whitespace scan clean; `git diff --check` only reports existing LF/CRLF warning for `.claude/CLAUDE.md`. | No runtime tests because this was documentation-only. `MPC/` remains untracked from prior scaffold. | Proceed to task #002 using `CONFIG.md` as required implementation input. |
| 2026-05-09 | Codex | Implemented repository boundary split requested by owner: moved Django backend, dashboard, Server docs, Server TODO/task history to `Server/`; made `Kalman/` a pure editable Python package `kalman`; moved pure ingestion, ARX prediction adapter, Adaptive Kalman filter, evaluation metrics, run config dataclass, and pure tests into `Kalman/kalman` / `Kalman/tests`; updated Server imports to depend on `kalman` and requirements to install both `../../Kalman` and `../../MPC`. | `Kalman/{pyproject.toml,README.md,TODO.md,.tasks/000-split-kalman-package-server.md,kalman/,tests/}`, `Server/`, `.claude/CLAUDE.md`, `MPC/CLAUDE.md`, `MPC/docs/technical/DESIGN_SYSTEM.md`, `.claude/.claude/review/REVIEW.md` | `python -m pytest Kalman\tests -q` -> 13 passed; `python -m compileall -q Kalman\kalman` OK; grep found no `django`/`rest_framework`/`estimation.models`/settings imports in `Kalman\kalman`; installed `greenhouse-kalman` editable with `python -m pip install -e Kalman`; `cd Server\backend; python manage.py check` OK; `python manage.py makemigrations --check --dry-run` no changes; `python -m compileall -q estimation config` OK; `cd Server\dashboard; npm test -- --run` -> 37 passed; `npm run build` OK with existing Vite chunk-size warning; `python -m pytest MPC\tests -q` -> 80 passed. | Server MySQL-backed gates are not fully complete because local MySQL at `127.0.0.1` refused connection: `python manage.py migrate --check` failed with OperationalError 2002, and full backend pytest showed 19 non-DB tests passed while DB tests errored during test DB setup. `Server/docs/technical/CODEBASE_ONBOARDING.md` intentionally remains stale per rule until owner review OK. `Server/.tasks/016...` remains in progress until DB-backed gates can be rerun. | Start MySQL/XAMPP and rerun `cd Server\backend; python manage.py migrate --check; python -m pytest estimation\tests -q`. After owner accepts the split, sync `Server/docs/technical/CODEBASE_ONBOARDING.md` to the new `Server/` + pure `Kalman/` boundary. |
| 2026-05-10 | Codex | Moved AMPC run/stop from frontend-local interval to persistent Django scheduler state. Added `AMPCSchedulerState`, migration `0009`, backend scheduler service with DB lock and daemon loop for serving process only, start/stop/status APIs, forecast `scheduler` contract, and frontend forecast button backed by API. Settings AMPC page keeps only config save; sensor ingest remains non-AMPC-triggering. | `Green-House/backend/api/{models.py,migrations/0009_ampc_scheduler_state.py,ampc_scheduler.py,apps.py,serializers.py,views.py,urls.py,tests/test_server_cutover.py}`, `Green-House/backend/config/settings.py`, `Green-House/frontend/src/app/api/endpoints.ts`, `Green-House/frontend/src/app/components/{ForecastPage.tsx,AutoSettings.tsx}`, `.claude/.claude/review/REVIEW.md` | `python manage.py migrate` OK; `python -m py_compile api\ampc_scheduler.py`; `python manage.py check` OK; `python manage.py makemigrations --check --dry-run` no changes; `python manage.py test api -v 1` -> 8 passed; `npm test` OK; `npm run build` OK; `python -m pytest Kalman\tests -q` -> 13 passed; `python -m pytest MPC\tests -q` -> 80 passed; `git diff --check` exit 0 with CRLF warnings only. | Scheduler is intentionally singleton/single-user for now; multi-user/multi-greenhouse isolation remains future work per owner. Background AMPC runs only while Django backend process is alive, not when backend service is stopped. `Green-House/docs/technical/CODEBASE_ONBOARDING.md` not updated until owner review approval. | User should reload backend after code change; if accepted, update onboarding/API docs for scheduler contract. |
| 2026-05-10 | Codex | Fixed AMPC low-forecast/Kalman drift reported from forecast dashboard. Root cause was live Kalman restoring `R=25` and using ARX as prior, which made `K≈0.02` and posterior follow bad ARX predictions instead of valid soil sensor readings; AMPC then used that bad posterior as control state/history. Added live-only Kalman tuning and R clamp, AMPC raw-sensor fallback when posterior diverges from raw, diagnostic state snapshot fields, and tightened sensor simulator soil range to 55-60. | `Green-House/backend/api/{estimation.py,ampc.py,tests/test_server_cutover.py}`, `Green-House/backend/config/settings.py`, `Green-House/backend/scripts/simulate_sensor_feed.bat`, `.claude/.claude/review/REVIEW.md` | Real DB inspection confirmed old recommendations used `kf_x_posterior=41.25` while raw soil was `60`, producing forecast `[23.3, 17.2, ...]`; rollback smoke after fix uses `control_soil_moisture` fallback. `python manage.py test api.tests.test_server_cutover -v 2` -> 10 passed; `python manage.py test api -v 1` -> 10 passed; `python manage.py check` OK; `python manage.py makemigrations --check --dry-run` no changes; `python -m pytest Kalman\tests -q` -> 13 passed; `python -m pytest MPC\tests -q` -> 80 passed; `npm test` OK; `npm run build` OK. | Existing bad `AMPCRecommendation` rows remain historical until AMPC generates a new recommendation; current DB also contains random simulator values outside 55-60 from the old script, including 48/50/67. Restart Django to load the new live Kalman settings. `CODEBASE_ONBOARDING.md` still not updated until owner review approval. | After restart, click `Dừng AMPC` then `Chạy AMPC` or wait next scheduler cycle; verify new `state_snapshot.control_soil_moisture` and forecast line stay near valid raw data. |
| 2026-05-10 | Codex | Lowered the shared Kalman adaptive measurement-noise cap from `R_max=25` to `R_max=15` in the pure Kalman package and taught MPC state handling to distrust Kalman posterior when a payload includes `kf_R > 15`, falling back to raw soil moisture or failing validation if raw is missing. Green-House AMPC now passes `kf_R` through the state snapshot/path so historical high-R cycles are guarded. | `Kalman/kalman/{filter/cycle.py,run_config/config.py}`, `Kalman/tests/test_kalman.py`, `MPC/mpc/state.py`, `MPC/tests/test_state.py`, `MPC/examples/demo_state.json`, `MPC/{README.md,docs/technical/API.md,docs/technical/ARCHITECTURE.md}`, `Green-House/backend/api/ampc.py`, `.claude/.claude/review/REVIEW.md` | `python -m pytest Kalman\tests -q` -> 15 passed; `python -m pytest MPC\tests -q` -> 82 passed; `python manage.py test api.tests.test_server_cutover -v 1` -> 10 passed; `python manage.py check` OK; `python manage.py makemigrations --check --dry-run` no changes; `python -m compileall -q Kalman\kalman MPC\mpc` OK; `git diff --check` only LF/CRLF warnings. | Backend live Kalman still has stricter runtime override `KALMAN_LIVE_R_MAX=4` from the drift fix, so new live rows may show R below 15 by design. The `15` cap is the shared package/MPC trust threshold and protects old/high-R state payloads. | If owner wants live backend to emit `kf_R` up to exactly `15`, retune `KALMAN_LIVE_Q` and `KALMAN_LIVE_R_MAX` together instead of raising R alone. |
| 2026-05-10 | Codex | Fixed pre-commit review findings for Green-House AMPC integration: `/api/auto-settings/` now proxies the default greenhouse `GreenhouseControlProfile` while preserving the existing frontend payload names, forecast no longer falls back to a global/other-greenhouse recommendation, AMPC scheduler recovers stale `is_executing` leases, and scheduler state is keyed per greenhouse for API-created states while the legacy `main` key remains fallback. | `Green-House/backend/api/{views.py,ampc_scheduler.py,tests/test_server_cutover.py}`, `.claude/.claude/review/REVIEW.md` | Added regression tests for auto-settings runtime profile update, forecast cross-greenhouse recommendation isolation, and stale scheduler lease recovery. `python manage.py test api.tests.test_server_cutover -v 2` -> 13 passed; `python manage.py test api -v 1` -> 13 passed; `python manage.py check` OK; `python manage.py makemigrations --check --dry-run` no changes; `python -m py_compile api\ampc_scheduler.py api\views.py` OK; `python -m pytest Kalman\tests -q` -> 15 passed; `python -m pytest MPC\tests -q` -> 82 passed; `npm test` OK; `npm run build` OK. | Existing database may still contain an old `AMPCSchedulerState(singleton_key='main')`; new API start/stop/status calls use `greenhouse:<id>` state. The background worker scans all enabled states, so disable stale `main` manually if it was left enabled in local DB. | Re-review before commit; if accepted, commit Green-House/Kalman/MPC changes only and keep deleted `Server/` review separate as requested. |
| 2026-05-12 | Codex | Reviewed MPC task #012 after owner request. Confirmed `GridShootingSolver` now chooses candidates through FAO `Dr/TAW/RAW` scoring and recommendation JSON carries `fao56` audit details, but found a remaining simulation-report mismatch: `run_simulation()` / `run_adaptive_simulation()` still compute `objective_cost` from legacy sensor-band errors while docs now describe FAO stress/overwater objective. | `MPC/mpc/solver/{grid.py,cost.py}`, `MPC/mpc/simulation/runner.py`, `MPC/mpc/simulation/report.py`, `MPC/tests/test_grid_solver.py`, `MPC/tests/test_simulation_regression.py`, `MPC/.tasks/012-fao56-dr-objective-recommendation.md`, `.claude/.claude/review/REVIEW.md` | `python -m pytest MPC\tests\test_grid_solver.py -q` -> 16 passed; `python -m pytest MPC\tests\test_fao56.py MPC\tests\test_cli_simulation.py -q` -> 51 passed; `python -m pytest MPC\tests -q` -> 117 passed; `python -m compileall -q MPC\mpc` OK; CLI smoke `config-schema`, `recommend`, `simulate`, `adaptive-simulate`, `auto` OK; generated smoke reports removed; `git diff --check -- MPC` only LF/CRLF warnings. | Finding: simulation metrics objective/cost_breakdown are not FAO despite docs/user guide claiming stress/overwater objective; tests only assert `overwater` key exists, not nonzero/correct. `CODEBASE_ONBOARDING.md` still intentionally not updated until owner review acceptance. | Fix simulation scoring or docs before accepting #012 if simulation report objective is part of the public FAO contract; otherwise explicitly document it as legacy report scoring. |
| 2026-05-12 | Codex | Re-reviewed MPC task #012 after fix. The previous simulation-report finding is resolved: `_score_with_daily_reset()` now delegates to `score_fao56_pump_sequence_with_daily_reset()`, reports include FAO `overwater`, and docs clarify `band_violation_*` is only a legacy sensor-percent diagnostic. No new blockers found. | `MPC/mpc/solver/cost.py`, `MPC/mpc/simulation/runner.py`, `MPC/mpc/simulation/report.py`, `MPC/tests/test_simulation_regression.py`, `MPC/docs/technical/{API,VALIDATION}.md`, `MPC/docs/user/USER_GUIDE.md`, `.claude/.claude/review/REVIEW.md` | `python -m pytest MPC\tests\test_simulation_regression.py -q` -> 4 passed; `python -m pytest MPC\tests\test_grid_solver.py MPC\tests\test_fao56.py MPC\tests\test_cli_simulation.py -q` -> 67 passed; `python -m pytest MPC\tests -q` -> 117 passed; `python -m compileall -q MPC\mpc` OK; CLI smoke `recommend` and `simulate --max-steps 3 --beam-width 4` OK; generated smoke reports removed; `git diff --check -- MPC` only LF/CRLF warnings. | `cost_breakdown.band` is retained as the backward-compatible key for FAO stress cost, so consumers should read docs rather than infer legacy band semantics from the key name. `CODEBASE_ONBOARDING.md` still intentionally not updated until owner review acceptance. | Accept #012 or, if desired, do a small follow-up docs/onboarding sync after owner approval. |
| 2026-05-12 | Codex | Reviewed Green-House task #003 `003-wire-ampc-fao-runtime`. Runtime now wires greenhouse FAO profile fields into MPC config, fetches hourly Open-Meteo ET0 before solve, stores ET0/FAO audit snapshots, preserves legacy frontend fields, and only queues actuator commands when AUTO/enabled/safe. | `Green-House/backend/api/{ampc.py,et0.py,models.py,serializers.py,views.py,tests/test_server_cutover.py,tests/test_et0.py}`, `Green-House/frontend/src/app/api/endpoints.ts`, `Green-House/docs/technical/AMPC_MODELING_HANDOFF.md`, `Green-House/.tasks/003-wire-ampc-fao-runtime.md`, `.claude/.claude/review/REVIEW.md` | `python manage.py test api -v 1` -> 29 passed; `python manage.py check` OK; `python manage.py makemigrations --check --dry-run` no changes; `python -m pip install -r requirements-local.txt --dry-run` OK; `python -m pytest MPC\tests -q` -> 117 passed; `python -m pytest Kalman\tests -q` -> 15 passed; frontend `npm test` OK; frontend `npm run build` OK. | Finding remains: `run_auto_recommendation()` calls `profile_to_config(profile)` before any fail-closed persistence. If persisted/admin-imported FAO config becomes invalid despite serializer validation, `Fao56Config`/`ControllerConfig` raises before an `AMPCRecommendation` audit row is stored, so scheduler/API can error instead of fail closed with pump 0. | Wrap initial runtime config construction in fail-closed handling or add a safe config-validation path that persists `model_error`/`invalid_config` with no command. |
| 2026-05-12 | Codex | Re-reviewed Green-House task #003 after invalid-config fail-closed fix. The previous finding is resolved: invalid persisted FAO/profile config now creates a `config_error` fail-closed audit with pump `0`, skips ET0/solver, records `state_snapshot.fail_closed`, and queues no actuator command. | `Green-House/backend/api/ampc.py`, `Green-House/backend/api/tests/test_server_cutover.py`, `.claude/.claude/review/REVIEW.md` | Targeted invalid-profile test passed; `python manage.py test api -v 1` -> 30 passed; `python manage.py check` OK; `python manage.py makemigrations --check --dry-run` no changes; `python -m pip install -r requirements-local.txt --dry-run` OK; `python -m pytest MPC\tests -q` -> 117 passed; `python -m pytest Kalman\tests -q` -> 15 passed; frontend `npm test` OK; frontend `npm run build` OK; `git diff --check -- Green-House/backend/api/ampc.py Green-House/backend/api/tests/test_server_cutover.py` only LF/CRLF warnings. | No new blockers found. The `config_error` reason prefix currently says `invalid_fao_config` even if a broader controller config field caused the error; acceptable for fail-closed behavior but can be renamed later for clarity. | Accept task #003 or proceed to the next Green-House FAO task after owner signoff. |
| 2026-05-12 | Codex | Reviewed Green-House task #004 `004-fao-auto-settings-frontend`. UI now exposes FAO controls, soil presets, theta warning text, save/loading/error states, and payload builder matching backend field names. Found validation/test-coverage gaps before acceptance. | `Green-House/frontend/src/app/components/{AutoSettings.tsx,autoSettingsConfig.ts}`, `Green-House/frontend/src/app/api/endpoints.ts`, `Green-House/frontend/scripts/smoke-test.mjs`, `Green-House/.tasks/004-fao-auto-settings-frontend.md`, `.claude/.claude/review/REVIEW.md` | `npm test` OK; `npm run build` OK; targeted backend auto-settings tests OK; `python manage.py check` OK; `python manage.py test api -v 1` -> 30 passed; `python -m pytest MPC\tests -q` -> 117 passed; `python -m pytest Kalman\tests -q` -> 15 passed; `git diff --check HEAD~1..HEAD` clean. | Findings: `validateAutoSettings()` does not validate `crop_kc`, cost weights, or `soft_daily_pump_cap_seconds`, so invalid values can be saved and later force AMPC into `config_error`; frontend smoke tests are static string checks and do not execute render/change/save/error behavior despite task acceptance claiming behavioral coverage. | Add executable tests for `applySoilPreset`, `buildAutoSettingsPayload`, validation, and error parsing/rendering; extend validation and preferably backend serializer checks for all runtime config fields MPC rejects. |
| 2026-05-12 | Codex | Re-reviewed Green-House task #004 after validation/test fix. Previous findings are resolved: client validation now rejects non-finite/negative runtime fields including `crop_kc`, weights, and daily cap; backend serializers validate shared runtime config; frontend smoke test executes helper behavior through Vite SSR and server-renders the loaded/error form. | `Green-House/frontend/src/app/components/{AutoSettings.tsx,autoSettingsConfig.ts}`, `Green-House/frontend/scripts/smoke-test.mjs`, `Green-House/backend/api/{serializers.py,tests/test_server_cutover.py}`, `.claude/.claude/review/REVIEW.md` | `npm test` OK; `npm run build` OK; targeted backend auto-settings tests OK; `python manage.py check` OK; `python manage.py makemigrations --check --dry-run` no changes; `python manage.py test api -v 1` -> 31 passed; `python -m pytest MPC\tests -q` -> 117 passed; `python -m pytest Kalman\tests -q` -> 15 passed; `git diff --check HEAD~1..HEAD` clean. | No blockers found. Remaining limitation: frontend smoke still is not a browser interaction test, but it now executes the critical pure helpers and render path, which is enough for this task's risk level. | Accept task #004 or proceed to #005 forecast/audit UI. |
| 2026-05-12 | Codex | Implemented Green-House task #005 `005-fao-forecast-audit-ui`. Forecast API types now include `state_snapshot.fao56`; forecast UI renders a compact FAO-56 audit panel while keeping the chart on sensor-percent `predicted_soil_moisture`; AMPC reason display now hides stack traces/file paths. | `Green-House/frontend/src/app/api/endpoints.ts`, `Green-House/frontend/src/app/components/ForecastPage.tsx`, `Green-House/frontend/scripts/smoke-test.mjs`, `Green-House/docs/technical/AMPC_MODELING_HANDOFF.md`, `Green-House/.tasks/005-fao-forecast-audit-ui.md`, `Green-House/TODO.md`, `.claude/.claude/review/REVIEW.md` | `npm test` OK; `npm run build` OK; `git diff --check -- Green-House/frontend/src/app/components/ForecastPage.tsx Green-House/frontend/src/app/api/endpoints.ts Green-House/frontend/scripts/smoke-test.mjs` clean apart from CRLF warnings. | No blockers found. Smoke coverage now SSR-renders the FAO audit panel with and without audit data, checks wet/safe/stress classification, verifies chart data still comes from percent predictions, and checks backend internals are not leaked in error text. | Proceed to task #006 after owner review. |
| 2026-05-12 | Codex | Reviewed Green-House task #005 `005-fao-forecast-audit-ui` after owner request. Confirmed frontend contract matches backend `AMPCRecommendationSerializer`, forecast chart remains based on top-level sensor-percent `predicted_soil_moisture`, FAO panel reads `state_snapshot.fao56`, and AMPC error display sanitizes stack/file details. | `Green-House/frontend/src/app/api/endpoints.ts`, `Green-House/frontend/src/app/components/ForecastPage.tsx`, `Green-House/frontend/scripts/smoke-test.mjs`, `Green-House/backend/api/{serializers.py,views.py}`, `.claude/.claude/review/REVIEW.md` | `npm test` OK; `npm run build` OK; `python manage.py check` OK; `python manage.py makemigrations --check --dry-run` no changes; `python manage.py test api -v 1` -> 31 passed; `python -m pytest MPC\tests -q` -> 117 passed; `python -m pytest Kalman\tests -q` -> 15 passed; `git diff --check HEAD~1..HEAD` clean. | No blockers found. Remaining limitation: forecast audit coverage is SSR/helper smoke rather than full browser interaction, but it exercises the risk points for this UI-only task. | Accept task #005 or continue to the next Green-House FAO task after owner signoff. |
| 2026-05-12 | Codex | Completed Green-House task #006 `006-fao-integration-gates-docs`. Ran final FAO-56 integration gates, applied pending local DB migration `api.0010_add_fao56_control_profile_config`, updated active MPC/Green-House docs, added Green-House user guide for FAO settings/forecast audit, and left `Green-House/docs/technical/CODEBASE_ONBOARDING.md` unchanged per owner-approval rule. | `Green-House/.tasks/006-fao-integration-gates-docs.md`, `Green-House/TODO.md`, `Green-House/docs/technical/AMPC_MODELING_HANDOFF.md`, `Green-House/docs/user/USER_GUIDE.md`, `MPC/docs/technical/{API.md,CONFIG.md,VALIDATION.md}`, `.claude/.claude/review/REVIEW.md` | Backend `python manage.py test api -v 1 --noinput` -> 31 passed; `python manage.py check` OK; `python manage.py makemigrations --check --dry-run` no changes; `python manage.py migrate --check` OK after applying local DB migration; `python -m pip install -r requirements-local.txt --dry-run` OK; frontend `npm test` OK; `npm run build` OK; `python -m pytest Kalman\tests -q` -> 15 passed; `python -m pytest MPC\tests -q` -> 117 passed; compileall gate OK; `git diff --check` clean apart from CRLF warnings. | No blockers found. Residual limitation: `CODEBASE_ONBOARDING.md` still intentionally reflects the pre-acceptance snapshot until owner explicitly approves onboarding sync. | Owner review/accept FAO-56 integration; after approval, run a follow-up onboarding sync for `Green-House/docs/technical/CODEBASE_ONBOARDING.md`. |
| 2026-05-12 | Codex | Reviewed Green-House task #006 `006-fao-integration-gates-docs` after owner request using architecture-review framing. Docs/gate record matches current FAO runtime contracts: Green-House profile maps into MPC FAO config, `state_snapshot.fao56` holds physical audit, top-level forecast remains sensor percent, and onboarding doc was intentionally left unchanged. | `Green-House/.tasks/006-fao-integration-gates-docs.md`, `Green-House/TODO.md`, `Green-House/docs/technical/AMPC_MODELING_HANDOFF.md`, `Green-House/docs/user/USER_GUIDE.md`, `MPC/docs/technical/{API.md,CONFIG.md,VALIDATION.md}`, `Green-House/backend/api/{ampc.py,views.py,tests/test_server_cutover.py}`, `.claude/.claude/review/REVIEW.md` | Re-ran gates: `npm test` OK; `npm run build` OK; `python manage.py check` OK; `python manage.py makemigrations --check --dry-run` no changes; `python manage.py test api -v 1 --noinput` -> 31 passed; `python manage.py migrate --check` OK; `python -m pip install -r requirements-local.txt --dry-run` OK; compileall gate OK; `python -m pytest Kalman\tests -q` -> 15 passed; `python -m pytest MPC\tests -q` -> 117 passed; `git diff --check HEAD~1..HEAD` clean. | No blockers found. Residual note: AMPC scoped endpoints have code-level ownership guards via `get_greenhouse_for_user()`, while direct cross-user tests are strongest for control-profile and forecast isolation; add explicit AMPC endpoint 404 tests later if tightening security evidence. | Accept task #006, then do the planned onboarding sync only after owner approval. |
| 2026-05-12 | Codex | Per owner request, ran a broad review of `Kalman/`, `MPC/`, and `Green-House/` using `.codex` skills `architect-review`, `bug-hunter`, and backend reliability checklist. Main findings are in `Green-House`: WebSocket auth/token checks are too permissive, dashboard/device/alert/sensor REST paths are not consistently greenhouse-owner scoped, pending commands are global, and tracked `*.egg-info` files make dependency dry-runs dirty the worktree. | `Kalman/`, `MPC/`, `Green-House/backend/api/{consumer.py,views.py,services.py,models.py}`, `Green-House/frontend/src/app/{contexts/RealtimeContext.tsx,components/SensorChart.tsx}`, `.claude/.claude/review/REVIEW.md` | Validation still passes despite findings: `python -m pytest Kalman\tests -q` -> 15 passed; `python -m pytest MPC\tests -q` -> 117 passed; `python manage.py test api -v 1 --noinput` -> 31 passed; `python manage.py check` OK; `python manage.py makemigrations --check --dry-run` no changes; `python manage.py migrate --check` OK; compileall gate OK; frontend `npm test` OK and `npm run build` OK; `python -m pip install -r requirements-local.txt --dry-run` OK after restoring generated egg-info metadata; `git diff --check` only CRLF warning on review memory. | Tests do not cover WebSocket consumers or cross-user legacy dashboard/device/alert endpoints, so the security findings are not contradicted by passing gates. `Kalman/` and `MPC/` package tests look healthy; remaining issues there are maintenance-level, chiefly tracked egg-info metadata and minor frontend TS rule drift. | Prioritize a Green-House security hardening task: require token/JWT on both websocket consumers, scope all dashboard/device/alert/sensor/command queries by greenhouse owner/device, add regression tests, then remove tracked egg-info from git. |
| 2026-05-12 | Codex | Fixed owner-requested BE/MPC/Kalman security findings while intentionally leaving FE client drift untouched. Backend WebSocket consumers now fail closed without valid frontend JWT or controller device token, REST dashboard/sensor/device/alert paths are greenhouse-owner scoped, pending command fetch/ACK is greenhouse/device scoped, `Device.code` uniqueness is per greenhouse, and tracked Kalman/MPC `*.egg-info` files were removed from git index with `*.egg-info/` ignored. | `Green-House/backend/api/{consumer.py,views.py,services.py,models.py,ampc.py,tests/test_server_cutover.py,migrations/0011_scope_device_code_per_greenhouse.py}`, `Green-House/backend/api/management/commands/seed_demo.py`, `.gitignore`, `Kalman/greenhouse_kalman.egg-info/`, `MPC/greenhouse_mpc.egg-info/`, `.claude/.claude/review/REVIEW.md` | `python manage.py test api -v 1 --noinput` -> 36 passed; `python manage.py check` OK; `python manage.py makemigrations --check --dry-run` no changes; `python manage.py migrate --noinput` applied `api.0011_scope_device_code_per_greenhouse`; `python manage.py migrate --check` OK; `python -m pytest MPC\tests -q` -> 117 passed; `python -m pytest Kalman\tests -q` -> 15 passed; `python -m pip install -r requirements-local.txt --dry-run` OK; `git diff --check` only CRLF warnings. | FE was deliberately not changed per owner instruction, so the current frontend WebSocket client will need a later FE-only update to pass JWT in the query string before realtime dashboard reconnects under the new fail-closed backend rule. | Commit/push backend hardening after owner review; then schedule the separate FE WebSocket token wiring if realtime UI is needed. |
| 2026-05-12 | Codex | Runtime-only review of backend/Kalman/MPC/Green-House after owner asked to ignore FE except hard runtime blockers. Found one backend boot blocker: `config.asgi` imports `api.routing` before setting `DJANGO_SETTINGS_MODULE` and before `get_asgi_application()`, so direct ASGI/Daphne import fails before websocket consumers can start. Kalman/MPC package tests and Django model/migration checks otherwise pass. | `Green-House/backend/config/asgi.py`, `Green-House/backend/config/wsgi.py`, `Green-House/backend/api/{consumer.py,services.py,models.py,ampc.py,ampc_scheduler.py,views.py,serializers.py,et0.py}`, `MPC/mpc/{fao56.py,config.py,state.py,solver/grid.py,solver/cost.py,plant/arx.py,cli.py}`, `Kalman/kalman/`, `.claude/.claude/review/REVIEW.md` | Reproduced blocker: `python -c "import config.asgi; print('asgi import ok')"` fails with `ImproperlyConfigured`; with `DJANGO_SETTINGS_MODULE=config.settings` preset it still fails with `AppRegistryNotReady: Apps aren't loaded yet.` Positive gates: `python -m pytest Kalman\tests -q` -> 15 passed; `python -m pytest MPC\tests -q` -> 117 passed; `python -m compileall -q Kalman\kalman MPC\mpc Green-House\backend\api Green-House\backend\config` OK; `python manage.py check` OK; `python manage.py makemigrations --check --dry-run` no changes; `python manage.py test api -v 1 --noinput` -> 36 passed; `python manage.py migrate --check` OK; `python -m pip install -r requirements-local.txt --dry-run` OK; MPC CLI smokes `config-schema`, `recommend`, `auto` OK and generated reports removed. | Review deliberately skipped frontend code/security per owner scope. WSGI import passes, so pure HTTP via WSGI is not the blocker; the affected path is ASGI/WebSocket/Daphne startup. | Fix `config/asgi.py` import order: set env first, call `get_asgi_application()` before importing websocket routing, then re-run `python -c "import config.asgi"` plus Django/API/Kalman/MPC gates. |
| 2026-05-12 | Codex | Continued runtime-only review. Confirmed proposed ASGI import order fixes the startup issue in an isolated import smoke, verified actual MySQL constraints for `api_device`, and found an edge scheduler runtime failure: `_scheduler_key_for_greenhouse(1000000000)` returns 21 chars while `AMPCSchedulerState.singleton_key` is `max_length=20`, causing MySQL `DataError` under strict mode. | `Green-House/backend/api/ampc_scheduler.py`, `Green-House/backend/api/models.py`, `Green-House/backend/api/migrations/0011_scope_device_code_per_greenhouse.py`, `.claude/.claude/review/REVIEW.md` | `python -c "import os; os.environ.setdefault('DJANGO_SETTINGS_MODULE','config.settings'); from django.core.asgi import get_asgi_application; django_asgi_app=get_asgi_application(); import api.routing; print('asgi reordered import ok')"` -> OK; Django shell DB introspection for `api_device` shows only `uq_device_greenhouse_code(greenhouse_id, code)` and `api_token` unique, so global `code` unique is removed; scheduler key probe shows `greenhouse:1000000000` length 21 vs max 20; insert smoke raises `DataError (1406, "Data too long for column 'singleton_key' at row 1")`; no test row remained. | Scheduler key overflow only appears once greenhouse ids reach 10 digits or if ids are manually imported that high, so current small local DB may not hit it. It is still a real runtime 500 path for scheduler start/status/stop at that id range. | Either increase `AMPCSchedulerState.singleton_key` to match generated keys or truncate/hash scheduler keys consistently like `ControlState` does, then add a regression test around long greenhouse ids. |
| 2026-05-12 | Codex | Fixed ASGI/Daphne startup blocker by moving `DJANGO_SETTINGS_MODULE` setup and `get_asgi_application()` before importing `api.routing`, then reusing `django_asgi_app` for HTTP in the protocol router. | `Green-House/backend/config/asgi.py`, `.claude/.claude/review/REVIEW.md` | Reproduced fixed path: `python -c "import config.asgi; print('asgi import ok')"` OK; with `$env:DJANGO_SETTINGS_MODULE='config.settings'` preset also OK; `python -m py_compile config\asgi.py` OK; `python manage.py check` OK; `python manage.py test api -v 1 --noinput` -> 36 passed; `python manage.py makemigrations --check --dry-run` no changes; `python manage.py migrate --check` OK. | No blockers found. JWT tests still emit the existing dev `SECRET_KEY` length warning only. | Ready for owner review or deploy with Daphne/ASGI startup. |
| 2026-05-13 | Codex | Fixed AMPC scheduler key overflow for large greenhouse ids. `_scheduler_key_for_greenhouse()` now preserves legacy `greenhouse:{id}` keys when they fit `max_length=20` and falls back to compact hex `gh:{id:x}` for high imported ids; scheduler tests now cover id `1_000_000_000`. Also re-verified current `config.asgi` already has the correct ASGI import order. | `Green-House/backend/api/ampc_scheduler.py`, `Green-House/backend/api/tests/test_server_cutover.py`, `Green-House/backend/config/asgi.py`, `.claude/.claude/review/REVIEW.md` | `python -c "import config.asgi; print('asgi import ok')"` OK; preset `$env:DJANGO_SETTINGS_MODULE='config.settings'` import OK; targeted scheduler large-id test OK; `python manage.py test api -v 1 --noinput` -> 37 passed; `python manage.py check` OK; `python manage.py makemigrations --check --dry-run` no changes; `python manage.py migrate --check` OK; `python -m pytest MPC\tests -q` -> 117 passed; `python -m pytest Kalman\tests -q` -> 15 passed. | No runtime blockers found in these BE/MPC/Kalman gates. Existing dev JWT warning remains due short local `SECRET_KEY`. | Owner can deploy ASGI build plus scheduler key fix; FE remains intentionally out of scope. |
