# MPC Design System

`MPC/` chưa có frontend/UI riêng trong v2. Dashboard vẫn nằm ở `Kalman/dashboard`.

Nếu sau này thêm UI cho recommendation hoặc actuator pilot, cần tạo design task trước khi implement frontend.

